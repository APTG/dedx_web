{
  "name": "dedx-web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "wasm:build": "cd wasm && ./build.sh",
    "wasm:verify": "node wasm/verify.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
