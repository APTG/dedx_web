### Proposed Decision

We should add a dedicated "Static Analysis & Linting" job to our GitHub Actions CI workflow (`.github/workflows/ci.yml`). This job will enforce our codebase's style and quality constraints by executing the `check`, `lint`, and `format:check` scripts already defined in our `package.json`.

### Rationale

1. **Best Practice & Code Quality:** Running linters (`eslint`) and formatters (`prettier`) in CI is an industry standard. It ensures that all merged code adheres to the project's style guidelines and catches potential issues early, preventing "formatting wars" in PR reviews.
2. **Fail Fast Principle:** By running static analysis early, we can fail the pipeline on trivial formatting or linting errors immediately. This saves time and CI runner minutes by avoiding the execution of heavier unit and E2E tests on malformed code.
3. **Consistency:** Enforcing these checks remotely guarantees that every contributor either configures their editor properly or runs the formatting scripts locally before pushing, leading to a much cleaner commit history.

### Action Plan

1. **Modify CI Workflow:** Edit `.github/workflows/ci.yml` to include a new job named `static-analysis` (or `lint`).
2. **Environment Setup:** Configure the job to run on `ubuntu-latest`, checkout the code, setup Node.js v24, setup `pnpm`, and install dependencies (`pnpm install --frozen-lockfile`).
3. **Add Verification Steps:** Add the following run steps to the new job:
   - `pnpm run format:check` (Prettier check)
   - `pnpm run lint` (ESLint check)
   - `pnpm run check` (TypeScript and Svelte sync checks)
4. **Optimize Pipeline:** Update the existing `unit-tests` job to depend on the new job (`needs: [workflow-guards, static-analysis]`). This ensures we don't run expensive tests if the code doesn't even pass basic linting and formatting.

---

_This comment was generated by AI (Gemini 3.1 Pro (High) via Antigravity)._
