# 2026-05-15 — Manual S3 E2E Suite

## Session Narrative

### Prompt 1: Implement CI test with runner-hosted S3 data

**AI response**: Investigated the existing external-data E2E suite, active CI workflow, Stage 8 tracking table, and recent AI logs. Confirmed the active Playwright tests referenced Cyfronet S3 directly and injected CORS headers in the browser route, which does not exercise a runner-hosted S3 service. Added a workflow-dispatch-only S3 E2E workflow that builds WASM, creates a deterministic synthetic `.webdedx` fixture, uploads it to runner-hosted MinIO, verifies the fixture and CORS response, and runs the external-data advanced-mode Playwright tests with `EXTERNAL_DATA_URL`.

## Tasks

### Runner-hosted S3 E2E coverage

- **Status**: completed
- **Stage**: 8 — Open Beta & User Feedback
- **Files changed**:
  - `.github/workflows/s3-e2e.yml`
  - `scripts/create-synthetic-webdedx-fixture.mjs`
  - `tests/e2e/external-data-advanced.spec.ts`
  - `package.json`
  - `playwright.config.ts`
- **Decision**: Use MinIO in a manual `workflow_dispatch` workflow so default PR CI no longer depends on Cyfronet reachability while still testing browser fetches against an S3-compatible endpoint. The fixture is generated during the workflow instead of committed as binary data.
- **Issue**: Full browser execution of the manual S3 suite still requires CI-built WASM; local verification covered fixture generation, zarrita reads, MinIO upload/public access, CORS response headers, and Playwright test discovery.

### Remove Cyfronet URLs from active tests

- **Status**: completed
- **Stage**: 8 — Open Beta & User Feedback
- **Files changed**:
  - `src/lib/utils/external-data-example-urls.ts`
  - `src/tests/unit/external-data-example-urls.test.ts`
  - `src/routes/docs/user-guide/+page.svelte`
- **Decision**: Replace the copy-paste example target with a neutral `example.com` `.webdedx` placeholder and update user-guide text to tell users to substitute their own hosted store. This keeps tests free of Cyfronet S3 URLs and avoids presenting a known CI-inaccessible endpoint as a test fixture.
- **Issue**: Historical logs and prototype reports still mention Cyfronet URLs as archival records; active test files no longer do.

### Project tracking and validation

- **Status**: completed
- **Stage**: 8 — Open Beta & User Feedback
- **Files changed**:
  - `docs/00-redesign-plan.md`
  - `CHANGELOG-AI.md`
  - `docs/ai-logs/README.md`
  - `docs/ai-logs/2026-05-15-ci-s3-data.md`
- **Decision**: Link PR #495 in the Stage 8 PR table because the issue is beta-feedback/CI hardening for external data.
- **Issue**: None.
