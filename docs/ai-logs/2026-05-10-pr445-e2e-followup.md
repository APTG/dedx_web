# 2026-05-10 — PR #445 E2E follow-up

**Model:** GPT-5.5 via GitHub Copilot coding agent

## Session Narrative

### Prompt 1: fix failing tests and keep Task 5 separate

User asked to implement Stage 6.10 Task 5 in a separate PR and fix failing tests reported from CI, including `pnpm test:e2e` failures.

**AI response**: Investigated the latest failed CI run (`25638079492`) with GitHub Actions MCP. The failing E2E job had two failures:

1. `tests/e2e/calculator-url.spec.ts` used `page.getByText(/Carbon \(C\)/)`, which became ambiguous because the selected particle trigger and the still-mounted dropdown option both contain `Carbon (C)`. The test now asserts against the Particle trigger text instead.
2. `tests/e2e/custom-compounds.spec.ts` included the LiF custom-compound calculation smoke test even though Stage 6.10 Task 5 (calculator dispatch through `calculateCustomCompound`) is explicitly being moved to a separate PR. The test is now skipped with a comment pointing to the missing Task 5 implementation. The stale energy-input selector in that skipped test was also updated to the current `data-testid="energy-input-0"` pattern so the test body is ready for re-enabling when Task 5 lands.

Validation performed:

- `pnpm build` — passed.
- `pnpm test -- --reporter=basic` — 51 files / 1000 tests passed.
- `pnpm exec playwright test tests/e2e/calculator-url.spec.ts tests/e2e/custom-compounds.spec.ts --grep "loading URL|LiF pellet"` — 1 passed, 1 skipped.
- `pnpm test:e2e` — 138 passed, 5 skipped.

## Tasks

### Fix latest E2E failures

- **Status**: completed
- **Stage**: 6.10 review follow-up
- **Files changed**:
  - `tests/e2e/calculator-url.spec.ts`
  - `tests/e2e/custom-compounds.spec.ts`
  - `CHANGELOG-AI.md`
  - `docs/ai-logs/README.md`
  - `docs/ai-logs/2026-05-10-pr445-e2e-followup.md`
- **Decision**: The LiF calculation smoke test was skipped instead of force-fitting behavior into this branch because the user explicitly requested Task 5 in a separate PR and the test depends on Task 5 calculator integration.
- **Issue**: Stage 6.10 Task 5 remains pending in the separate PR branch.
