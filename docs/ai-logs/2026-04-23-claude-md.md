# 2026-04-23 — Add CLAUDE.md (Claude Code entry point)

## Session Narrative

### Prompt 1: Based on copilot-instructions.md add CLAUDE.md, think what should go there and what should go to agents.md. Important to keep there reminder about AI logs.

**AI response**: Read `.github/copilot-instructions.md` in full, then read `AGENTS.md`
and `opencode.json` to map the three-tool ecosystem (Copilot → copilot-instructions.md,
opencode/Qwen → AGENTS.md, Claude Code → nothing yet).

**Content split decision:**

- `CLAUDE.md` — self-contained; no "go read another file" indirection since Claude Code
  loads it automatically at session start. Covers everything Claude needs cold: stack,
  Svelte 5 rules, code style, build commands + WASM caveats, key docs index, working
  process, cross-spec consistency rules, and the AI logging section with the correct
  Claude-specific attribution string `(Claude Sonnet 4.6 via Claude Code)`. Deliberately
  omits opencode-specific content (PLGrid credentials, npx MCP servers, vendor offline
  paths, Qwen branch naming) — that stays in AGENTS.md.

- `AGENTS.md` — kept as-is for opencode/Qwen; added a single callout note redirecting
  Claude Code users to CLAUDE.md.

- `copilot-instructions.md` — unchanged; remains the Copilot-specific version and the
  canonical logging-format reference.

**AI logging prominence:** The AI Session Logging section in CLAUDE.md is marked with a
⚠ emoji heading, placed last so it is easy to find, includes the full changelog row
format with mandatory `(Claude Sonnet 4.6 via Claude Code)` attribution, the session
log template, and index-maintenance reminders.

## Tasks

### Create CLAUDE.md

- **Status**: completed
- **Stage**: 5 (housekeeping / project tooling)
- **Files changed**:
  - `CLAUDE.md` (new)
  - `AGENTS.md` (one-line Claude redirect added)
- **Decision**: Made CLAUDE.md self-contained rather than a pointer to
  copilot-instructions.md. Pointer files work when the target is always read first;
  Claude Code reads only CLAUDE.md automatically, so the content must be present
  directly. Kept the section order logical: rules first, process second, logging last
  (easy to find at the bottom).
- **Issue**: none.
