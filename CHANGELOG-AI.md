# AI Changelog

> This changelog tracks all AI-assisted coding sessions on this project.
> Each entry links to a detailed session log in `docs/ai-logs/`.
>
> **Note:** AI-assisted work on this project began on 1 April 2026 with
> the creation of the redesign plan (`docs/00-redesign-plan.md`). Those
> early planning sessions pre-date this changelog and are not recorded here.
> This log starts from 3 April 2026 when the AI changelog system was introduced.

## Entry format

Use one bullet per session (newest first):

- `<YYYY-MM-DD>` — **<stage>**: <description> (`<model> via <tool>`)
  - **Log:** [log](docs/ai-logs/YYYY-MM-DD-<slug>.md)

## Entries (newest first)

- 2026-06-05 — **Stage 8 / accessibility**: WCAG audit + fixes (Issues #708–#711). Added a skip-to-content link with an e2e assertion (SC 2.4.1, not caught by axe), a global `:focus-visible` fallback (SC 2.4.7), a `<header>` banner landmark with `<nav>` scoped to the route tabs (SC 1.3.1), extended the axe e2e to the `wcag22aa` tag set with 24px clear-button hit areas (SC 2.5.8), a global `prefers-reduced-motion` rule, and darkened `--muted-foreground` for contrast headroom (SC 1.4.3). One commit per issue. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-06-05-wcag-accessibility-audit.md)

- 2026-06-04 — **Docs / libdedx migration**: Added `docs/12-libdedx-migration-plan.md` — a phased plan to migrate C helpers from `wasm/dedx_extra.c` upstream into `libdedx`. Includes a per-symbol inventory (which of the 810 LOC belong in `dedx.h` / `dedx_tools.h` / `dedx_wrappers.h`), flags the duplicated material display-name table (C vs `src/lib/config/material-names.ts`) and the inverse-STP branch-selection bug that should be fixed upstream, and sequences the work across libdedx issues (#79, #51, #48, #6, #86, #95) and the dedx_web follow-ups (#175, #176, #504). Filed the upstream issues: tracking epic libdedx#118 with per-phase sub-issues libdedx#119–#123 (Phase F = existing #79), cross-referenced to the health-review epic #108 to avoid duplication. Indexed in `docs/README.md`. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-06-04-libdedx-migration-plan.md)

- 2026-06-04 — **Stage 8 / code-health**: Back the calculator's recompute-on-access getters with `$derived` (Issue #689). `rows`, `stpDisplayUnit`, and `validationSummary` in `src/lib/state/calculator.svelte.ts` previously ran `computeRows()` / `getStpDisplayUnit()` / `computeValidationSummary()` on **every** property access — re-parsing every row several times per frame and handing back a fresh object each read. Replaced them with module-level `$derived` values (matching `entity-availability.svelte.ts`), with `validationSummary` deriving from the memoized `rows` so a single parse pass backs both. Public getter surface unchanged; added referential-stability unit tests. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-06-04-issue-689-derived-getters.md)

- 2026-06-03 — **Code health / cleanup**: Removed the dead legacy entity-selection component tree (Issue #688) — `entity-combobox.svelte`, `entity-selection-comboboxes.svelte`, `entity-selection-panels.svelte`, `entity-panel.svelte` and their four test files. These were superseded by the `entity-selection/` subtree and were no longer imported by the app (only referenced by their own tests and one stale comment). Repointed the dangling comment in `utils/program-kind.ts` to describe the grouping logic directly. ~1,250 LOC of dead code + 4 dead test files removed; lint/check/format and the full unit suite pass. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-06-03-entity-selection.md)

- 2026-06-03 — **Docs / Stage 8**: Sync `docs/03-architecture.md` with the Stage 8 codebase (Issue #631). Rewrote the project-structure tree, reactive-state topology, component tree, data-flow diagrams, error-handling, accessibility, and related-docs sections to reflect the real modules — factory-function state (`createEntitySelectionState`/`createCalculatorState`/…) + page orchestrators, `app-init.svelte.ts` bootstrap, the layered URL parser (`url-grammar.peggy` → AST, `urlv=2`), the tabbed `entity-selection/` picker, `results/table-*.svelte`, `unit-anchor-strip.svelte`, and `<svelte:boundary>` page error fallbacks. Removed the dead `EntityDropdown`/`EntityPanel`/`EnergyInput`/`UnitSelector`/`entities.svelte.ts`/`calculation.svelte.ts` references. Annotated the shipped/deferred follow-ups in `entity-selection.md`; verified `calculator.md` already uses the unit-anchor strip. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-06-03-issue-631-docs-sync.md)

- 2026-06-01 — **Custom Compounds**: Implemented compound formula fallback UI for weight fractions and complex mass ratios (Issue #676). Updated `displayElements` to pass raw element counts in weight mode so the heuristic calculates formulas and defaults to "no simple formula" when complex. (Gemini 3.1 Pro via Antigravity)
  - **Log:** [log](docs/ai-logs/2026-06-01-issue-676-formula-display.md)

- 2026-06-01 — **Testing**: Add edge case testing for external data validation (Issue #630). Added comprehensive unit tests for `validation.ts` covering malformed JSON, NaN, Infinity, negative boundary conditions, and improperly typed fields. (Gemini 3.1 Pro (High) via Antigravity)
  - **Log:** [log](docs/ai-logs/2026-06-01-issue-630-validation-tests.md)

- 2026-05-31 — **Stage 8 / inverse-lookups**: Extend the advanced-mode inverse tabs so each emits a second output quantity, mirroring the `Energy →` (STP, Range) layout (Issue #673). **Range →** gains a `→ STP` column (stopping power at the resolved energy, with the shared `StpUnitHeaderMenu` / `stpOutputUnit` output unit, persisted via `sunit=`). **STP →** pairs a `→ Range` column with each energy branch — high-E always shown, low-E pair revealed together when a row has two solutions. The inverse WASM calls only echo the input quantity, so the complementary value is recovered with a forward `calculate()` / `calculateCustomCompound()` at the resolved energy/energies (one extra forward call for Range →, two for STP →) — no new WASM API. State (`RangeRow.stoppingPower`, `InverseStpRow.rangeLow/HighCm`), calc orchestrators, both table components, and the `inverse-lookups.md` (v7) / `calculator.md` / `06-wasm-api-contract.md` specs updated; new unit tests for the derived outputs. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-31-inverse-lookups.md)

- 2026-05-30 — **Stage 8 / calculator**: Add a stopping-power **output unit** selector to the Advanced calculator (Issue #670). The STP column header is now a dropdown (`STP (keV/µm) ▾` single-entity / `Stopping Power (…) ▾` multi-entity) offering `keV/µm` · `MeV/cm` · `MeV·cm²/g`, with a desktop popover and a mobile bottom sheet (≥44 px rows). The choice is encoded in the shared `sunit=` URL param (single source of truth for both calculator and plot; plot migrated off `stp_unit=` but still reads it as a legacy fallback) and carries across in-app navigation via shared in-memory state — **no `localStorage`** (per request). Single-entity STP conversion moved to render time (engine stores raw mass `MeV·cm²/g`; `getStpDisplayUnit()` honours an explicit override over the aggregate-state default), so a unit switch re-renders without recalculating. Unknown/missing `sunit` falls back to `keV/µm`. Added unit, component, and Playwright tests. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-30-stp-output-units.md)

- 2026-05-30 — **WASM / Testing**: Bumped `libdedx` submodule to latest `main` and rebuilt WASM. Added E2E tests (`tests/e2e/pr667-water.spec.ts`) for custom compounds using analytical programs, validating Custom Water against Predefined Liquid Water (ID 276). Also verified table stability when dynamically adding and removing energies. (Antigravity via opencode)
  - **Log:** [log](docs/ai-logs/2026-05-30-pr667-custom-water-stability.md)

- 2026-05-29 — **WASM**: Bump `libdedx` submodule to `6313d91` to pick up the merged fix [APTG/libdedx#105](https://github.com/APTG/libdedx/pull/105) ("Fix double-call bug for custom compounds with analytical programs"), which resolves [#662](https://github.com/APTG/dedx_web/issues/662) (Bethe/Bethe-ext returned no values for custom compounds). Also corrected the submodule tracking branch from the now-deleted `master` to `main`, and added an E2E regression test (`tests/e2e/url-parser.spec.ts`) exercising the CR39 custom compound with the Bethe program. WASM is gitignored and rebuilt from the submodule by CI/deploy, so no committed binary changes. (Claude Opus 4.8 via Claude Code)

- 2026-05-29 — **Stage 6.10**: Implement the mobile compound editor (Issue #647 / #606-4) — a phone-only 2-step full-screen sheet (Basics → Composition) that replaces the cramped desktop modal on `(max-width: 640px) and (pointer: coarse)`. Adds a full-screen periodic-table picker overlay, a long-press row action sheet, a compressed duplicate banner, and virtual-keyboard handling (`interactive-widget=resizes-content` + a `--vkb-offset` visual-viewport fallback). All form state stays in the parent behind a shared `EditorController`, so step transitions are non-destructive; desktop layout is unchanged. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-29-issue-647-compound-editor-mobile.md)

- 2026-05-29 — **Refactoring**: Address PR #659 review — fully honor "rejected, not migrated" by gating URL hydration on version negotiation in both page orchestrators. An unsupported `urlv` now hydrates nothing (entity selection, energy rows, advanced mode, plot series stay at defaults) instead of only halting the calc; "Load defaults" proceeds from those defaults. Plot version negotiation moved to a synchronous pre-effect step so it resolves before init/restore; `setupPlotUrlRestore` gained a mismatch gate. Formal spec §3.1/§3.4 wording tightened. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-29-issue-477-url-version-gating.md)

- 2026-05-29 — **Refactoring**: Refactor URL parser to the §6 layered architecture (Issue #477) — executable Peggy grammar (`url-grammar.peggy`) → AST (`url-ast.ts`) via `parseQuery`, semantic `resolveCalculatorState`/`resolvePlotState`, shared helpers in `url-shared.ts` (de-duplicates the `mat_*`/last-wins/strict-number blocks), and span-accurate diagnostics (`url-diagnostics.ts`) for precise error messages. Retires v1 (rejected with an "unsupported link" banner, not migrated) and adds a `migrateUrl` seam. Behaviour preserved; published `/docs/technical` grammar page and formal spec updated. (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-29-issue-477-url-parser.md)
- 2026-05-29 — **Testing**: Add dedicated tests for LibdedxServiceImpl directly (Issue #629). Tested memory allocation, buffer management, error boundary handling, and correct type conversions. (Gemini 3.1 Pro via Antigravity)
  - **Log:** [log](docs/ai-logs/2026-05-29-issue-629.md)
- 2026-05-29 — **Stage 5**: Implement Quick Start panel & default to LiF (Antigravity via Google DeepMind)
  - **Log:** [log](docs/ai-logs/2026-05-29-issue-646.md)
- 2026-05-29 — **Testing**: Clean up legacy URL tests in `calculator-url.test.ts` (Issue #632): labeled all `urlv=1` fixtures as legacy migration tests, updated non-migration tests to use `urlv=2`, added 8 new v2 tests covering `mode=basic`, `across=programs` encode/decode, `urlv=2` canonical decode, and `qfocus=both` migration round-trips. (Claude Sonnet 4.6 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-29-issue-632-url-test-cleanup.md)

- 2026-05-29 — **Stage 6.10**: Reimplemented compound editor live derived UI & mass-% validation (Issue #645) cleanly on master — pure `$derived` Save gating, formula footer (Unicode formula · atom count · Bragg I-value), weight-fraction sum tracker with auto-rescale, and live per-row mass-%. Fixes the duplicate-element Save-gating regression from PR #653 (the formula footer's keyed `{#each}` crashed on duplicate Z, leaving Save enabled). (Claude Opus 4.8 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-29-issue-645-derived-ui.md)

- 2026-05-28 — **Stage 6.10**: Implemented periodic-table picker for compound editor and duplicate resolution logic (Issue #644). (Gemini 2.5 Pro via Antigravity)
  - **Log:** [log](docs/ai-logs/2026-05-28-issue-644.md)

- 2026-05-28 — **Refactoring**: Extract "Availability Computation" and "External Compatibility Context" into `entity-availability.svelte.ts` (#650). (Gemini 3.1 Pro via Antigravity)
  - **Log:** [log](docs/ai-logs/2026-05-28-issue-650-extract-availability.md)

- 2026-05-28 — **Refactoring**: Decompose `calculator.svelte.ts` and `entity-selection.svelte.ts` state monoliths into `calculator-engine.svelte.ts` and `multi-selection.svelte.ts`, with related export-button accessibility updates in `+layout.svelte`. (Antigravity via opencode)
  - **Log:** [log](docs/ai-logs/2026-05-28-issue-628.md)

- 2026-05-28 — **Testing**: Address issue #635 stale builds in Playwright. Documented the stale-build trap and the rule to keep `package.json` E2E scripts clean while relying on Playwright `webServer.command` to run `pnpm build && pnpm preview` locally (non-CI). Created a `playwright.dev.config.ts` targeting Vite dev server on port 5173 for local HMR testing. (Gemini 3.1 Pro (High) via Antigravity)
  - **Log:** [log](docs/ai-logs/2026-05-28-issue-635-stale-builds.md)

- 2026-05-27 — **Refactoring**: Decompose `src/routes/plot/+page.svelte` and refactor `src/routes/calculator/+page.svelte` by extracting page orchestration into `PlotPageOrchestrator` / `CalculatorPageOrchestrator` plus shared calculator components (`page-error-fallback`, `advanced-hint`, `shared-compound-alert`) (#627). (Antigravity via opencode)
  - **Log:** [log](docs/ai-logs/2026-05-27-issue-627.md)

- 2026-05-27 — **Refactoring**: Extract duplicated utilities — `slug()` to `src/lib/export/utils.ts`, `asBuiltinMaterial`/`asBuiltinParticle` to `src/lib/utils/entity-type-guards.ts`, `validateEntityArray()` helper inside `validation.ts` (#626). (Claude Sonnet 4.6 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-27-issue-626-extract-duplicate-utilities.md)

- 2026-05-27 — **Stage 5 — entity-selection**: Remove "common particles" concept — drop star icon, bold emphasis for proton/alpha, and Common particles/Ions section headers; all particles now rendered uniformly sorted by Z (#622). (Claude Sonnet 4.6 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-27-issue-622-remove-common-particles.md)

- 2026-05-27 — **Testing**: Add unit tests for custom-compounds and inverse-lookups (#621). (Qwen3.5-397B via opencode)
  - **Log:** [log](docs/ai-logs/2026-05-27-issue-621.md)

- 2026-05-27 — **Testing**: Add unit tests for headless calc setup functions extracted in PR #619 (`setupMultiProgramCalculation`, `setupMultiEntityCalculation`, `setupInverseRangeCalculation`, `setupInverseStpCalculation`, `setupPlotPreviewCalculation`, `inverse-units`); 44 new tests across 5 new test files + 1 helper. (Claude Sonnet 4.6 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-27-state-store-unit-tests.md)

- 2026-05-27 — **Refactoring**: Decompose `/calculator` (1815 → 1055 lines) and `/plot` (1280 → 802 lines) page components by extracting calculation orchestration into headless `setupMultiProgramCalculation`, `setupMultiEntityCalculation`, `setupInverseRangeCalculation`, `setupInverseStpCalculation`, `setupPlotPreviewCalculation`, and `setupPlotUrlRestore` state modules; extracted pure helpers (`inverse-units`, `energy-anchor-options`, `calculator-pdf-metadata`, `plot-image`); deduplicated `resolveExtLocalIdForLabel` into `$lib/external-data/ids.ts`. (Claude Sonnet 4.6 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-27-page-decomposition.md)

- 2026-05-26 — **Stage 5 — entity-selection**: Advanced search operators for picker tabs (#609): `z>N`/`z>=N`/`z<N`/`z<=N` for particles, `ρ>N`/`ρ>=N`/`ρ<N`/`ρ<=N`/`ρ=N` (+ `rho` alias) for materials, `tag=fn`/`tag=tab`/`tag=data`/`tag=ext`/`v=<string>` for programs. 13 new unit tests. Spec updated. (Claude Sonnet 4.6 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-26-issue-609-advanced-search-syntax.md)

- 2026-05-26 — **Stage 5 — entity-selection**: Decompose `particle-tab.svelte` into focused `particle-list-view` and `particle-grid-view` sub-components (#615) (Claude Opus 4.7 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-26-issue-615-particle-tab-decompose.md)

- 2026-05-26 — **Stage 5 — entity-selection**: Address PR #614 review-thread fixes for particle grid, URL `across` canonicalization, and E2E stability (GPT-5.5 via GitHub Copilot coding agent)
  - **Log:** [log](docs/ai-logs/2026-05-26-pr614-review-thread-fixes.md)

- 2026-05-26 — **Stage 5 — entity-selection**: Wire multi-particle selection into URL as `particles=` + `across=particle` (#599 follow-up) (Claude Sonnet 4.6 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-26-issue-599-periodic-grid-particle-picker.md)

- 2026-05-26 — **Stage 5 — entity-selection**: Implement periodic-grid scan view in advanced particle picker (#599) (Claude Opus 4.7 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-26-issue-599-periodic-grid-particle-picker.md)

- 2026-05-26 — **Refactoring**: Decompose `result-table.svelte` god component into mode-specific sub-components and extract column-reorder drag-and-drop into a reusable Svelte 5 action (#604) (Claude Opus 4.7 via Claude Code)
  - **Log:** [log](docs/ai-logs/2026-05-26-issue-604-decompose-result-table.md)

- 2026-05-25 — **Refactoring**: Extract async initialization logic into app-init state module (#603) (antigravity via opencode)
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-603-component-extraction.md)

- 2026-05-25 — **Refactoring**: Extract URL synchronization logic into headless stores (#602) (Gemini 1.5 Pro via opengravity)
  - **Log:** [log](docs/ai-logs/2026-05-25-url-sync-refactor.md)

- 2026-05-25 — **CI / infra**: **Issue #595 — Migrate project to pnpm v11** (Claude Sonnet 4.6 via Claude Code): Updated `packageManager` in `package.json` from `pnpm@10.33.0` to `pnpm@11.3.0`. Migrated `pnpm-workspace.yaml` from the deprecated `onlyBuiltDependencies` list syntax to the new `allowBuilds` map syntax introduced in pnpm v11. Type checks and unit tests passed, but the linked session log still recorded 3 `guard-forbidden-files` failures, so this session was not fully green.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-595-pnpm-v11.md)
- 2026-05-25 — **CI / docs**: **PR #596 follow-up — migrate AI changelog to bullet-list format** (GPT-5.5 via GitHub Copilot coding agent): investigated failing static-analysis formatting logs, replaced the markdown-table `CHANGELOG-AI.md` layout with a formatter-stable bullet-list structure, and updated AI instruction docs (`.github/copilot-instructions.md`, `CLAUDE.md`, `AGENTS.md`, `docs/opencode-setup.md`, `docs/00-redesign-plan.md`, `.opencode/lessons-learned.md`) to require prepending bullet entries instead of table rows.
  - **Log:** [log](docs/ai-logs/2026-05-25-changelog-bullet-format.md)
- 2026-05-25 — **Stage 8 / Docs**: **Issue #563 — Spec updates + 7 ADRs + formatting guideline** (Claude Sonnet 4.6 via Claude Code): Created ADRs 007–013 in `docs/decisions/` covering: drop-columns-dropdown, drop-unit-button, no-autofocus, inline-unit-grammar, compare-across-strip, inverse-stp-sticky-high-e, mode-tab-naming. Updated `calculator.md` (v9), `multi-program.md` (v4), `export.md` (v7), `entity-selection.md` to match shipped Stage 8 behaviour: `qfocus=` → `qshow=`, `hidden_programs=` removed, Columns… dropdown replaced, unit-anchor-strip documented, tab names Energy→/Range→/STP→, CSV always exports both quantities. Added `pnpm format` before-commit guideline to `CLAUDE.md`, `AGENTS.md`, and `.github/copilot-instructions.md`.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-563-docs-adrs.md)
- 2026-05-25 — **CI / infra**: **Issue #587 — Add static-analysis job to GitHub CI** (Claude Sonnet 4.6 via Claude Code): Added a `static-analysis` job to `.github/workflows/ci.yml` that runs `pnpm run format:check`, `pnpm run lint`, and `pnpm run check` after `workflow-guards`. Updated `unit-tests` to depend on `static-analysis` so expensive tests are skipped when linting or type-checking fails.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-587-ci-linter.md)
- 2026-05-25 — **CI / infra**: **Issue #591 — Fix `pnpm run format:check`** (Claude Sonnet 4.6 via Claude Code): Extended `.prettierignore` to exclude git submodules (`libdedx/`, `vendor/`), all `pnpm-lock.yaml` files (lock files are managed by pnpm, not prettier — best practice), and `docs/ai-logs/` (AI session logs contain raw tool output with pseudo-code `...` truncation markers that trip prettier's TypeScript parser with a hard `SyntaxError`). Ran `prettier --write .` to fix formatting in ~80 remaining source, test, doc, and config files. `pnpm run format:check` exits 0.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-591-fix-format-check.md)
- 2026-05-25 — **CI / infra**: **Issue #589 — Fix `pnpm run check` errors** (Claude Sonnet 4.6 via Claude Code): Fixed 47 svelte-check/tsc errors across 5 files. `units.ts`: changed `=== null` guards to `== null` so `noUncheckedIndexedAccess` array elements are narrowed correctly. `CsvExportModal.test.ts`: typed vi.fn() mocks with explicit generics matching the component prop signatures. `load-external-modal.svelte`: introduced `DataTransferItemWithFSA` intersection type for the non-standard File System Access API method. `picker-sheet.svelte`: removed unused `closeButtonEl` binding, widened `programMatches` parameter to `version?: string \| undefined`, used conditional spread for optional `iValue`. `plot/+page.svelte`: replaced `invStpBranch: cond ? "both" : undefined` with conditional spread to satisfy `exactOptionalPropertyTypes`. `pnpm run check` exits 0.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-589-fix-check-errors.md)
- 2026-05-25 — **Stage 8 / #562**: **Issue #562 — Row keyboard contract + cold-load arrow key tab switching** (Claude Sonnet 4.6 via Claude Code): Added `moveRow(index, direction)` to `EnergyInputState` and `CalculatorState`. Implemented full keyboard contract in `table-basic.svelte` and `table-advanced.svelte`: `Esc` blurs, `Shift+Enter` saves without adding a row, `Backspace` on empty deletes + focuses previous, `ArrowUp/Down` moves focus between rows, `Cmd/Ctrl+ArrowUp/Down` reorders + follows focus. Added global `ArrowLeft`/`ArrowRight` handling in `entity-selection.svelte` so tabs switch on cold load (body focus) or when focus is in the picker but not on a tab button or input.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-562-row-keyboard.md)
- 2026-05-25 — **CI / E2E**: **Issue # (manual) S3 E2E tests are failing** (GPT-5.5 via GitHub Copilot coding agent): investigated failed `Manual S3 E2E` run `26401454732` and updated `tests/e2e/external-data-advanced.spec.ts` to match the current advanced tabbed picker contract (`across-program`, `picker-tab-program`, `picker-program-list`) instead of the removed `Programs` button/listbox flow. Added helper coverage for opening the program picker consistently before SRIM assertions.
  - **Log:** —
- 2026-05-25 — **CI / infra**: **Issue #584 — Fix lint errors** (Claude Sonnet 4.6 via Claude Code): Removed unused `getParticleLabel` import from `particle-tab.svelte`; replaced two `delete g[dynamicKey]` calls with `Reflect.deleteProperty` in `jsroot-plot.svelte`; removed unused `RANGE_ANCHOR_OPTIONS` constant from `+page.svelte`; removed unused `ICRU73_OLD_ID` constant from `calculator-advanced.spec.ts`. `pnpm lint` exits 0.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-584-fix-lint.md)
- 2026-05-25 — **CI / infra**: **Issue #580 — Review and simplify pnpm overrides in pnpm-workspace.yaml** (Gemini 1.5 Pro via Antigravity): Reviewed package overrides and removed redundant overrides for `svelte` and `devalue` since transitive dependency upgrades resolve them securely. Kept and simplified the `cookie` override from `>=0.7.0` to `^0.7.0` (mitigating GHSA-pxg6-pf52-xh8x) with a YAML comment referencing SvelteKit tracking. Verified pnpm audit is clean, check/lint/test/build succeed, and all 31 E2E smoke tests pass. Added Dependency Management & Security Policy to README.md.
  - **Log:** [log](docs/ai-logs/2026-05-25-issue-580-review-pnpm-overrides.md)
- 2026-05-25 — **CI / docs**: **PR #581 follow-up — remove redundant deploy.json workflow steps and clarify E2E build requirements** (GPT-5.5 via GitHub Copilot coding agent): verified that `pnpm build` now generates `static/deploy.json` via the `prebuild` hook, removed redundant `Write deploy.json` steps from `ci.yml`, `s3-e2e.yml`, and `deploy.yml`, and updated the testing/deployment docs plus README to clarify that `pnpm test:e2e` still requires a prior `pnpm build` because Playwright serves the built app with `pnpm preview`.
  - **Log:** [log](docs/ai-logs/2026-05-25-pr581-workflow-doc-clarification.md)
- 2026-05-25 — **CI / E2E**: **PR #581 follow-up — fix real mobile overflow from footer build metadata** (GPT-5.5 via GitHub Copilot coding agent): investigated the newest shard-3 CI failure on commit `303060b`, confirmed the remaining responsive regression was a real mobile overflow rather than rounding drift, and fixed the shared footer/build-info layout so long `deploy.json` branch metadata wraps on mobile instead of forcing horizontal overflow. Revalidated `tests/e2e/responsive.spec.ts` on `mobile-chrome` and the exact shard-3 Playwright command after installing Firefox locally.
  - **Log:** [log](docs/ai-logs/2026-05-25-pr581-footer-responsive.md)
- 2026-05-25 — **CI / local dev**: **PR #581 follow-up — generate deploy.json automatically for local builds** (GPT-5.5 via GitHub Copilot coding agent): explained that `deploy.json` is the footer build-info metadata file consumed by `BuildInfoBadge`, confirmed the CI workflows no longer log `/deploy.json` 404s, and moved local generation into the normal build lifecycle with a `prebuild` hook so `pnpm build && pnpm test:e2e` no longer needs a manual `node scripts/deploy.cjs`. Added a lessons-learned entry for shared generated-asset setup.
  - **Log:** [log](docs/ai-logs/2026-05-25-pr581-local-deploy-info.md)
- 2026-05-25 — **CI / E2E**: **PR #581 follow-up — add 1 px tolerance to responsive overflow assertions** (GPT-5.5 via GitHub Copilot coding agent): investigated the latest failing shard-3 CI run on commit `68ff244`, confirmed the same three `mobile-chrome` responsive checks were still failing only in CI, and updated `tests/e2e/responsive.spec.ts` to poll numeric horizontal overflow with a 1 px tolerance instead of a raw boolean comparison. Added a lessons-learned entry for CI-only responsive rounding drift.
  - **Log:** [log](docs/ai-logs/2026-05-25-pr581-responsive-tolerance.md)
- 2026-05-25 — **CI / merge sync**: **PR #581 follow-up — merge latest master and revalidate responsive E2E** (GPT-5.5 via GitHub Copilot coding agent): merged `origin/master` (bringing in issue #561 compare-across changes), resolved the `CHANGELOG-AI.md` conflict by preserving both local and upstream rows, and revalidated the responsive mobile Playwright suite against the merged tree with the CI `wasm-binaries` artifact-backed preview (`5 passed, 1 skipped`). Also recorded a lessons-learned entry about avoiding stale preview-server reproductions.
  - **Log:** [log](docs/ai-logs/2026-05-25-pr581-merge-sync.md)
- 2026-05-25 — **CI / E2E**: **PR #581 follow-up — stabilize mobile responsive overflow assertions** (GPT-5.5 via GitHub Copilot coding agent): investigated the failing PR CI run, confirmed only `tests/e2e/responsive.spec.ts` failed on shard 3/3 while `master` was green on the same UI code, and replaced one-shot `scrollWidth > clientWidth` checks with `expect.poll(...)` so the assertions wait for post-load layout settling on mobile Chromium. Also updated lessons learned for responsive overflow tests and renumbered the earlier changelog-merge entry.
  - **Log:** —
- 2026-05-25 — **CI / infra**: **PR #577 merge-conflict resolution against master** (GPT-5.5 via GitHub Copilot coding agent): merged `origin/master` into the branch, preserved the upstream `ci.yml` 3-shard E2E updates, kept the `Write deploy.json` steps in `ci.yml` and `s3-e2e.yml`, and resolved the resulting `CHANGELOG-AI.md` conflict.
  - **Log:** —
- 2026-05-25 — **CI / infra**: **Issue #577 — fix [404] GET /deploy.json in E2E tests on GHA** (Claude Sonnet 4.6 via Claude Code): Added `node scripts/deploy.cjs` step to the `e2e-tests` job in `ci.yml` and to `s3-e2e.yml`, before `pnpm build`, mirroring the existing step in `deploy.yml`. The file was gitignored and only generated during the deploy workflow, so every E2E page load produced a noisy 404 in the WebServer log.
  - **Log:** —
- 2026-05-24 — **Stage 8**: **Issue #561** — compare-across 4-button strip, table-multi, drop Columns dropdown, 3→2 state quantityFocus shrink (Claude Sonnet 4.6 via Claude Code): Added `"single"` to `AcrossDimension` type; shrunk `QuantityFocus` from `"both"\|"stp"\|"csda"` to `"stp"\|"range"` across multi-program and multi-entity state; renamed URL param `qfocus→qshow`, silently dropped `hidden_programs=` on decode. Created `compare-across-strip.svelte` (4-pill radiogroup strip), `quantity-toggle.svelte` (2-button segmented control), and `table-multi.svelte` (single-quantity multi-entity grid with sticky-left energy column and yellow-tint default entity). Removed Columns… dropdown from `result-table.svelte` and `+page.svelte`. Wired `<CompareAcrossStrip>` and `<QuantityToggle>` in calculator page; routed multi-entity to `<TableMulti>`. Updated 5 test files; added E2E `tests/e2e/multi-entity.spec.ts`.
  - **Log:** [log](docs/ai-logs/2026-05-24-issue-561-compare-across-strip.md)
- 2026-05-24 — **Stage 8 / review fixes**: **PR #575 review-thread fixes + CI E2E follow-up** (GPT-5.5 via GitHub Copilot coding agent): fixed inverse-STP table review issues by cleaning up low-E reveal timer teardown, making STP branch auto-collapse reversible after 2-solution rows clear, and rendering delete controls as present-but-disabled when one row remains. Preserved `inv_stp_branch=both` in plot URL round-trips and added plot URL codec tests for the new param. Updated the inverse-lookups URL round-trip E2E expectation to keep the restored typed range value (`3.5`).
  - **Log:** [log](docs/ai-logs/2026-05-24-pr575-review-thread-fixes.md)
- 2026-05-24 — **Stage 8 / #560**: **Issue #560 — Inverse-STP table (high-E default, sticky low-E reveal, 2-series plot)** (Claude Sonnet 4.6 via Claude Code): Created `src/lib/utils/inverse-stp.ts` with `HIGH_E_SIDE`/`LOW_E_SIDE` constants and `StpBranchState` type. Extended `InverseLookupState` with `stpBranchState`, `setStpBranchState`, and `removeStpRow`. Built `table-inverse-stp.svelte` — high-E column by default; low-E column reveals with one-time amber tint when any row returns 2 solutions; auto-collapses when all rows return to single-solution; `?istpbranch=both` pre-opens the second column. Added `istpBranchState` encode/decode to `calculator-url.ts`. Replaced ≈85-line inline STP section in `+page.svelte` with new component. Added `labelSuffix` to `PlotSeriesData` to support two legend entries per entity; wired `inv_stp_branch=both` detection in `plot/+page.svelte`. Fixed pre-existing `ivalues:`→`lookups:` URL-encoding bug. Added 7 unit tests and 8 E2E tests.
  - **Log:** [log](docs/ai-logs/2026-05-24-inverse-stp-table.md)
- 2026-05-24 — **Stage 8 / review fixes**: **PR #572 CI/export follow-up + issue #573 docs URLs** (GPT-5.5 via GitHub Copilot coding agent): fixed the failing export Advanced Playwright suite by updating it to the single-program `TableAdvanced` contract, restored single-program Advanced recalculation on advanced-option changes in `src/routes/calculator/+page.svelte`, aligned additional E2E suites (`advanced-options`, `calculator`, `calculator-advanced`, `complex-interactions`, `particle-unit-switching`, `url-parser`) with the new Advanced single-table selectors/behavior, and updated the user-guide example URLs to current `urlv=2` syntax with extra basic/advanced examples for issue #573. Revalidated with the full Playwright matrix: 245 passed, 13 skipped.
  - **Log:** [log](docs/ai-logs/2026-05-24-pr572-export-followup.md)
- 2026-05-24 — **Stage 8 / review fixes**: **PR #572 review-thread fixes + E2E stabilization** (GPT-5.5 via GitHub Copilot coding agent): applied all Copilot review feedback on the combined Advanced table by fixing heavy-ion energy-unit option detection in `table-advanced.svelte`, adding accessible labels for Range → inputs, and correcting the `formatStpValue` docs. Fixed the cited Playwright failures by routing single-program Advanced mode to `TableAdvanced`, aligning inverse-range tests with the current result-cell contract, removing the stale `across=none` deep link, and checking in the missing ARIA snapshot.
  - **Log:** [log](docs/ai-logs/2026-05-24-pr572-review-thread-fixes.md)
- 2026-05-24 — **Stage 8 / #559**: **Issue #559 — Combined Advanced result table for Energy → and Range → modes** (Claude Sonnet 4.6 via Claude Code): Created `table-advanced.svelte` — single combined input+output grid for Advanced single-entity mode. Energy → mode: columns ≡ · # · ENERGY · → MeV/nucl (conditional on isPerRowMode) · STP · RANGE · ×; row 1 faint yellow tint; sticky-left first column. Range → mode: same component shape with RANGE · UNIT · → Energy columns. Removed the standalone `UnitAnchorStrip` between picker and results for single-entity Advanced mode (unit selection now lives inside the component). Replaced the ≈90-line inline Range → grid in `+page.svelte` with `<TableAdvanced mode="range">`. Added `removeRow` to `CalculatorState` and `removeRangeRow` to `InverseLookupState`. Extended `value-formatters.ts` with `formatStpValue` and `formatEnergy`. This is the moment the legacy `energy-unit-selector.svelte` Advanced call sites disappear from single-entity mode; the file can be deleted in #563.
  - **Log:** [log](docs/ai-logs/2026-05-24-combined-advanced-table.md)
- 2026-05-24 — **Stage 8 / UI polish**: **Issue #570 — Compact calculator header for mobile** (Claude Sonnet 4.6 via Claude Code): Reduced ~58px of vertical dead space above entity selection on 390px phones. `pt-3 pb-6 sm:py-6` on `<main>` in layout; outer wrapper `space-y-3 sm:space-y-6`, merged heading+description into one block (`text-xl sm:text-2xl font-semibold`, description `hidden min-[400px]:block text-xs sm:text-sm`), inner content wrapper `space-y-4 sm:space-y-6` in calculator page. Desktop layout unchanged via `sm:` variants.
  - **Log:** [log](docs/ai-logs/2026-05-24-compact-calculator-header.md)
- 2026-05-23 — **Stage 8 / review fixes**: **PR #569 review-thread fixes + E2E stabilization** (GPT-5.5 via GitHub Copilot coding agent): addressed all Copilot review comments for the unit-anchor-strip PR by hardening `uanchor` decode with an own-property check, clarifying `energyAnchor` semantics, stopping the calculator page from emitting `energyAnchor` without distinct UI state, adding URL codec coverage for `uanchor`, updating inverse-lookups Playwright tests to use the new radiogroup contract, and adding baseline `UnitAnchorStrip` component tests. Also added a new lessons-learned entry about updating E2E contracts when replacing native form controls with custom accessible widgets.
  - **Log:** [log](docs/ai-logs/2026-05-23-pr569-review-thread-fixes.md)
- 2026-05-23 — **Stage 8 / #558**: **Issue #558 — Mode tab rename + unit-anchor strip** (Claude Sonnet 4.6 via Claude Code): renamed Advanced mode tabs from Forward/Range/Inverse STP to Energy→/Range→/STP→ with desktop sublabels and sub-400px mobile single-letter glyphs. Built new `unit-anchor-strip.svelte` (pill-strip radiogroup with tooltips + badge support, reusable for the plot page). Replaced `energy-unit-selector.svelte` (deleted) and the Range/STP `<select>` dropdowns with the anchor strip. Added `(≠MeV)` badge on MeV/u for proton in Advanced mode. Added proton MeV/u support in `getAvailableEnergyUnits` (Advanced mode). Added `uanchor=` URL round-trip in `calculator-url.ts`.
  - **Log:** [log](docs/ai-logs/2026-05-23-mode-tabs-unit-anchor.md)
- 2026-05-23 — **Stage 8 / E2E fixes**: **PR #568 follow-up — url-parser E2E regressions** (GPT-5.5 via GitHub Copilot coding agent): fixed `tests/e2e/url-parser.spec.ts` expectations after URL major bump to v2 by updating “current version” coverage (`urlv=2`), updating advanced/custom-compound/unknown-param URLs to `urlv=2`, and refreshing scenario descriptions for legacy missing-`urlv` behavior. Verified with Playwright URL parser suite (9 passed with WASM).
  - **Log:** [log](docs/ai-logs/2026-05-23-pr568-url-parser-e2e-followup.md)
- 2026-05-23 — **Stage 8 / review fixes**: **PR #568 review-thread fixes + E2E stabilization** (GPT-5.5 via GitHub Copilot coding agent): applied all Copilot review comments by bumping URL negotiation major to 2, hardening Basic inline-unit hint storage/timer cleanup in `table-basic.svelte`, and switching Basic energy inputs to `inputmode="text"`. Fixed failing Playwright specs for current behavior (`lookups`/inverse round-trip expectations, explicit `+ Add row` behavior in Basic mode, and Basic-mode column test flow). Added PR #568 entry to the Stage 8 table in `docs/00-redesign-plan.md`.
  - **Log:** [log](docs/ai-logs/2026-05-23-pr568-review-thread-fixes.md)
- 2026-05-23 — **Input grammar**: **Issue #557 — Inline-unit parser, Basic mode layout fix, URL v2 `lookups=` rename** (Claude Sonnet 4.6 via Claude Code): Created `src/lib/parse/inline-unit.ts` with pure-function range and STP parsers (ASCII-only input — `um` for µm, `keV/um` for keV/µm) plus URL-token helpers. 42 unit tests in `src/tests/unit/inline-unit.test.ts`. Fixed Basic mode layout: `updateRowText` gains an `autoAdd` flag (default `true`); Basic mode calls it with `false` so the card layout stays intact until Enter or the `+ Add row` button is clicked. Added `inputmode="decimal"` to Basic energy inputs and `inputmode="text"` to Advanced inputs. Added first-focus onboarding hint (localStorage-persisted, auto-dismiss after 10 s). Renamed `ivalues=` → `lookups=` in `calculator-url.ts` (URL v2, backward-compat read of v1 `ivalues=`), bumped `CALCULATOR_URL_VERSION` to 2. Updated all affected tests.
  - **Log:** [log](docs/ai-logs/2026-05-23-inline-unit-parser.md)
- 2026-05-23 — **Basic mode**: **Issue #556 — Basic mode simplification** (Claude Sonnet 4.6 via Claude Code): Created `table-basic.svelte` (3-column table + single-row card, ghost note for unit change, per-cell auto-scaled CSDA range) and `value-formatters.ts`. Modified `switchParticle` in `calculator.svelte.ts` to branch on mode: Advanced keeps KE-conservation conversion, Basic preserves typed values and auto-sets `masterUnit` to MeV/MeV/nucl based on particle. Wrapped `EnergyUnitSelector` in Advanced-mode guard. Updated `calculator-state.test.ts` KE-conservation tests to run in Advanced mode; added Basic-mode preservation tests.
  - **Log:** [log](docs/ai-logs/2026-05-23-basic-mode-simplification.md)
- 2026-05-23 — **Foundations**: **Issue #555 — Cold-load defaults E2E tests** (Claude Sonnet 4.6 via Claude Code): Audited issue #555 against current source; confirmed all runtime defaults (100 MeV row, proton + Water + Auto program, URL-params-win logic, no autofocus) are already implemented. Added `tests/e2e/cold-load-defaults.spec.ts` with 4 Playwright tests covering every acceptance criterion: `@smoke` pre-populated row value, `@smoke` results appear without interaction, `@responsive` no-autofocus on desktop+mobile, `@regression` `energies=` URL param overrides default.
  - **Log:** [log](docs/ai-logs/2026-05-23-cold-load-defaults.md)
- 2026-05-23 — **Schema work**: **PR #565 review follow-up — restore explicit Basic/Advanced URL mode** (GPT-5.5 via GitHub Copilot coding agent): Updated the v2 URL contract to keep `mode=basic\|advanced` explicit, move calculator operation to `calc=forward\|range\|inverse-stp`, and support advanced compare-across lists for `particles=`, `materials=`, and `programs=` gated by `across=`. Synced formal ABNF/semantic rules, entity-selection and v1-schema banners, ADR 006 rationale, PR table summary, and calculator URL test comments/type-safe assertions. Follow-up clarified the `calc=` token-reference heading, v1 `program=`/`programs=` migration precedence, and advanced `across=none` conformance-vector notes. Targeted calculator URL tests pass; `pnpm check` remains blocked by unrelated pre-existing diagnostics.
  - **Log:** [log](docs/ai-logs/2026-05-23-pr565-mode-review.md)
- 2026-05-23 — **Schema work**: **PR #565 round 5 — cross-spec sync, case-sensitivity, 15-token energy cross product, dual-output future plan** (Claude Sonnet 4.6 via Claude Code): Addressed review comments: fixed `entity-selection.md` §3.4 + §6 contradiction with v2 (advanced mode is inferred from `programs=` vs `program=`, not from a `mode=advanced` URL token); fixed conformance vectors #3 and #4 in `shareable-urls-formal.md` so each has a single unambiguous canonical string. Switched `uanchor=` value casing to physics-standard mixed case (`MeV`, `MeV/nucl`, `MeV/u`) to avoid the lowercase-`mev` / millielectronvolt confusion. Added §1.3 **Case Sensitivity Policy** to `shareable-urls.md` covering all token categories. Expanded per-row energy unit suffix token set to the full **15-token cross product** (5 prefixes `eV`/`keV`/`MeV`/`GeV`/`TeV` × 3 suffixes none/`/nucl`/`/u`). Replaced misleading `{csv}` placeholder with `{value-list}` in the canonical URL form. Added a forward-compatibility note in §3.4 about the planned dual-output extension for advanced inverse modes (range→stp+energy, stp→range+energy via additive `qshow=` tokens). Added a "⚠ URL examples use v1 schema — see shareable-urls.md for v2" deprecation banner to `multi-program.md`, `advanced-options.md`, `inverse-lookups.md`, `custom-compounds.md`, `calculator.md`, `stage-6-13-url-parser.md`. Added PR #565 row to Stage 8 PRs table in `00-redesign-plan.md`. All 61 calculator-url unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-22-url-schema-v2.md)
- 2026-05-23 — **Schema work**: **PR #565 round 4 — merge url-schema.md into shareable-urls.md; add migration modal spec** (Claude Sonnet 4.6 via Claude Code): Rewrote `shareable-urls.md` as the v2 canonical URL spec (v7) — merged all content from the separately-maintained `url-schema.md` (now deleted), added per-param sections for every v2 param, and added the v1 URL detection banner + future-version blocking modal spec (§7.2). Rewrote `shareable-urls-formal.md` (v7) with updated ABNF grammar (v2 params + deprecated v1 pairs marked with `;(deprecated)`), v1→v2 migration mapping table, updated semantic rules (advanced mode inferred from `programs=` vs `program=`), v2 canonicalization algorithm, and new migration conformance vectors. Deleted `url-schema.md`. Updated feature-specs README, test file doc comment, and session log.
  - **Log:** [log](docs/ai-logs/2026-05-22-url-schema-v2.md)
- 2026-05-23 — **Schema work**: **PR #565 round 3 — rename `ivalues=` → `lookups=`; trim unchanged-param sections** (Claude Opus 4.7 via Claude Code): Renamed the inverse-lookup input-list URL param from `ivalues=` to `lookups=` because the `i` prefix invited confusion with `ival=` / `mat_ival=` (the I-value / mean excitation potential from the Bethe-Bloch formula) — the two are unrelated. Added migration rule (v1 `ivalues=` accepted on read, copied verbatim into `lookups=`). Aggressively trimmed the `url-schema.md` sections for params unchanged from v1 (each gets one line in §3.1 plus a row in the §2 delta table; full per-param sections are reserved for v2-specific params). ADR 006 gained a §4 justifying the rename + listing the alternatives considered. Added an `ivalues=` round-trip migration-fixture test. Updated PR description, issue #554 description, AI session log, README. All 61 calculator-url unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-22-url-schema-v2.md)
- 2026-05-23 — **Schema work**: **PR #565 review — drop `Id` rename; extend `runit` to `dm`/`km`** (Claude Opus 4.7 via Claude Code): Reverted the `particle=`/`material=`/`program=` → `*Id` rename in `url-schema.md` v2 and ADR 006 §3 after maintainer pushback (rationale: link-rot cost outweighs cosmetic readability gain). Extended `runit=` token set to `nm\|um\|mm\|cm\|dm\|m\|km` to cover sub-mm and kilometre-scale ranges. Addressed Copilot review feedback: split deprecated v1 params into their own §3.17 section, fixed §3.7 worked-example bug (removed nonsense `mode=range&mode=advanced`), made `energies=`/`ivalues=` conditional on the active `mode=` in the canonical URL form, renamed two misleading migration-fixture test titles to match what they actually assert. Updated issue #554 description with the revision. All 60 calculator-url unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-22-url-schema-v2.md)
- 2026-05-22 — **Schema work**: **Issue #554 — URL schema v2 design doc + ADR** (Claude Sonnet 4.6 via Claude Code): Created `docs/04-feature-specs/url-schema.md` with the full v2 calculator-route query-parameter schema (all params, types, migration rules, one worked example per param); created `docs/decisions/006-url-schema-v2.md` (ADR justifying `hidden=` removal and `qfocus=` → `qshow=` rename); added 7 v1→v2 migration fixture tests to `calculator-url.test.ts` referencing the new schema doc as source of truth. No behaviour change. All 60 calculator-url unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-22-url-schema-v2.md)
- 2026-05-18 — **Stage 8 / picker**: **Issue #533 follow-up — Fix ghost ○ circles + remove (anchor) label** (Claude Sonnet 4.6 via Claude Code): `isMultiMode` in particle-tab and material-tab was not gated on `isAdvancedMode`, so `○` circles and `(anchor)` text lingered after returning to basic mode. Added `isAdvancedMode.value &&` guard (matching program-tab's `showAdvancedToolbar` pattern). Removed `(anchor)` label from all 6 picker files. Updated 3 unit tests that call `setAcross()` to enable advanced mode. Added PR #540 to Stage 8 redesign-plan table. All 1379 tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-18-issue533-picker-multimode-fix.md)
- 2026-05-18 — **Stage 8 / picker**: **Issue #533 — Compact sticky summary bar + orange selection indicators** (Claude Sonnet 4.6 via Claude Code): Replaced the confusingly identical grey selected-badge + list-row highlight with a shared `picker-summary-bar.svelte` component (orange count badge, truncated label list, Clear button, optional All/Sel-only toggle) and orange ring+checkmark styling for selected list rows. Applied consistently across particle-tab, material-tab, program-tab, program-inline-list, grouped-result-list, and picker-sheet. Color palette aligned to existing tab-bar orange (#f97316). All 1379 tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-18-issue533-picker-summary-bar.md)
- 2026-05-18 — **Stage 8 / plot**: **Issue #538 — Fix plotting permanently: load jsroot via UMD bundle** (Claude Sonnet 4.6 via Claude Code): Root-cause analysis showed jsroot 7.11.0 added `import('@resvg/resvg-js')` in `BasePainter.mjs`; the local shim alias changed Rollup's chunk-split decisions, exposing a pre-existing circular dependency (`ObjectPainter.mjs:1828` runs before `core.mjs` initialises `internals`). All five retry attempts from #537 failed because the error is structural, not timing-based. Fix: load jsroot's own pre-built UMD bundle (`jsroot.min.js`) via a `<script>` tag through a `getJsroot()` singleton, bypassing Rollup entirely. Added `copy-jsroot-bundle` Vite plugin to copy `node_modules/jsroot/build/jsroot.min.js → static/jsroot.min.js` on every dev/build run; removed `drawWithRetry`; updated tests to set `globalThis.JSROOT` in `beforeAll`. 1379 tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-18-issue538-jsroot-umd-bundle.md)
- 2026-05-18 — **Stage 8 / plot**: **Issue #536 — JSROOT plot stuck on "Loading plot engine…" forever** (Claude Sonnet 4.6 via Claude Code): The previous fix (2dc1592) silently swallowed the JSROOT module-race error without retrying, leaving the plot permanently on the loading placeholder. Added `drawWithRetry` to `jsroot-plot.svelte` — wraps `drawPlot` with up to 5 attempts and exponential back-off (0/100/200/400/800 ms), checking cancellation between retries. Simplified the outer catch (jsroot-race branch removed). Strengthened two E2E regression tests to assert the loading placeholder disappears and tagged them `@firefox` for Firefox Playwright project coverage.
  - **Log:** [log](docs/ai-logs/2026-05-18-issue536-jsroot-retry.md)
- 2026-05-18 — **Stage 8 / plot**: **Issue #534 — Firefox plotting error + source map path fix** (Claude Sonnet 4.6 via Claude Code): Broadened JSROOT module-race catch to match Firefox's error phrasing (`can't access property "jsroot"`) in addition to Chrome's, eliminating the "Failed to load the plot engine" banner on Firefox. Added `sourcemapPathTransform` to `vite.config.ts` so source map URLs include the `/web_dev/` base path when `BASE_PATH` is set.
  - **Log:** [log](docs/ai-logs/2026-05-18-issue534-plotting-error.md)
- 2026-05-18 — **Stage 8 / mobile+plot**: **PR #532 review follow-up (round 2) — add-series + JSROOT + ARIA consistency** (GPT-5.5 via GitHub Copilot coding agent): addressed latest review comments by treating `addSeries()` false as partial failure in multi-add, clearing stale `jsrootError` for transient `"reading 'jsroot'"` branch, centralizing max-series guard (`MAX_PLOT_SERIES=20`) and applying it consistently to both strip and sidebar add entry points, and fixing SeriesStrip list semantics by scoping `role=\"list\"` to rows-only container. Also broadened `/` editable guard to `[contenteditable]`, added a new lessons-learned entry, and re-ran lint, unit tests, and targeted Playwright suites (keyboard-nav, plot-add-series, responsive on chromium/mobile-chrome).
  - **Log:** [log](docs/ai-logs/2026-05-18-pr532-review-followup-round2.md)
- 2026-05-18 — **Stage 8 / mobile+plot**: **PR #532 review + CI follow-up — reviewer fixes and E2E stabilization** (GPT-5.5 via GitHub Copilot coding agent): addressed all requested review items by (1) clearing JSROOT plot state when no drawable data and tightening empty-data guards, (2) validating numeric IDs in plot multi-add flow with user-visible partial-failure messaging, (3) scoping `/` keyboard shortcut to avoid stealing focus from editable elements, (4) restoring list semantics on `SeriesStrip`, and (5) switching `search-input-keyboard` unit tests to `userEvent.setup()`. Fixed CI E2E regressions by updating keyboard-nav and responsive specs to current picker behavior and breaking a Plot URL-sync `replaceState` feedback loop that blocked exiting series edit mode. Re-ran lint, unit tests, and targeted Playwright suites (chromium + mobile-chrome).
  - **Log:** [log](docs/ai-logs/2026-05-18-pr532-review-followup.md)
- 2026-05-18 — **Stage 8 / mobile+plot**: **Issue #530 Part B — Plot add-series flow, keyboard shortcuts, JSROOT crash fix** (Claude Sonnet 4.6 via Claude Code): Fixed JSROOT `ObjectPainter.mjs` empty-draw crash (guard added; "Loading" placeholder stays until proton+water preview arrives). Resolved Part A deferred choices: ★ prefix for named particles in both inline list and picker sheet; slide-up 200ms sheet animation with prefers-reduced-motion fallback. Added `/` keyboard shortcut to focus search (expands panel if collapsed). Added keyboard shortcuts table to user guide. Added `updateSeries()` to `PlotState`. Created `SeriesStrip` component with live-edit mode (clicking series copies entities to picker for live mutation). Re-laid out plot page: removed old mobile disclosure pattern, uses `collapsible` picker + SeriesStrip. Added `handleAddOrMulti`/`handleAddMultiSeries` for Advanced mode multi-create. All 1379 unit tests pass; added `search-input-keyboard.spec.ts`, `keyboard-nav.spec.ts`, `plot-add-series.spec.ts`.
  - **Log:** [log](docs/ai-logs/2026-05-18-plot-add-series-flow.md)
- 2026-05-17 — **Stage 8 / mobile**: **PR #531 review follow-up — adaptive picker fixes + mobile E2E selector disambiguation** (GPT-5.5 via GitHub Copilot coding agent): addressed review items by pairing mobile sheet `pushState` with cleanup `history.back()` on UI-close, removing duplicated particle `Z=` badge in sheet rows, deleting unused `materialSubtab`/`scrollPositions`/`setMaterialSubtab` state API surface, making material sub-tab pill counts query-consistent, simplifying grouped Custom section rendering, and routing tiny-list auto-select via canonical `selectionState.selectProgram(-1)`. Fixed failing Playwright strict-mode conflict by scoping `picker-particle-item-6` assertions to `picker-sheet`. Added PR #531 entry to Stage 8 table and updated lessons learned with overlay test-id scoping guidance.
  - **Log:** [log](docs/ai-logs/2026-05-17-pr531-review-followup.md)
- 2026-05-17 — **Stage 8 / mobile**: **Issue #530 PR A — Adaptive Picker Kit + Particle/Material/Program Rework** (Claude Sonnet 4.6 via Claude Code): Introduced `size-bucket.ts` (tiny/medium/large thresholds). Reworked particle-tab to a flat Z-sorted list with Z inline in name (`proton (Z=1)`). Replaced material three-column layout with sub-tab pill controls (Compounds/Elements/Custom) with localStorage persistence and scroll memory. Added `ProgramInlineList` for tiny-bucket programs (≤10). Created `PickerSheet` full-screen search overlay for mobile with focus trap, body scroll lock, and hardware-Back support. Made `SearchInput` a tap-target button on mobile. Added jsdom guard for `window.matchMedia`. Added `size-bucket.spec.ts` (12 tests) and `mobile-picker.spec.ts` E2E. All 1368 tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-17-mobile-picker-part-a.md)
- 2026-05-17 — **Stage 8 / mobile**: **PR #529 review follow-up — selector contract + documentation corrections** (GPT-5.5 via GitHub Copilot coding agent): Updated header wrapper selector to `data-testid="app-header"` to match issue/test contract, corrected inconsistent Playwright status wording (180 passed, 10 skipped, 1 pre-existing failure) in changelog/log index, corrected the session-log note about deferred issue #528 acceptance-criteria scope, and added PR #529 to the Stage 8 table in the redesign plan.
  - **Log:** [log](docs/ai-logs/2026-05-17-pr529-review-followup.md)
- 2026-05-17 — **Stage 8 / mobile**: **Mobile nav visual hierarchy — issue #528** (Claude Sonnet 4.6 via Claude Code): Restructured `+layout.svelte` into two explicit rows: row 1 = logo + mode toggle chip + export buttons (desktop only) + share URL; row 2 = full-width Calculator / Plot / Docs route tabs with 48 px touch targets on mobile, filled active background, 2 px accent border, and `aria-current="page"` on the active tab. Playwright run result: 180 passed, 10 skipped, 1 pre-existing `url-parser` failure on master unrelated to this change.
  - **Log:** [log](docs/ai-logs/2026-05-17-mobile-nav-hierarchy.md)
- 2026-05-16 — **Stage 8 / mobile**: **PR #525 review follow-up: material-sheet accessibility + conditional fade hint** (GPT-5.5 via GitHub Copilot coding agent): Updated `material-tab.svelte` mobile full-screen sheet to lock body scroll while open, move focus to the close button, trap Tab focus inside the dialog, and restore focus to the originating `⤢` trigger on close/Escape. Made bottom fade overlays conditional on actual list overflow and hidden at scroll bottom. Added regression unit coverage for body scroll lock and focus restore behavior. Re-ran lint, unit tests, and build.
  - **Log:** [log](docs/ai-logs/2026-05-16-mobile-nav-material-review-followup.md)
- 2026-05-16 — **Stage 8 / mobile**: **Mobile nav two-row layout + Material-tab card polish** (Claude Sonnet 4.6 via Claude Code): Split the top nav into two rows on mobile: Row 1 shows icon (text hidden) + Calculator/Plot/Docs; Row 2 shows Basic/Advanced toggle + Share URL. Added `overscroll-y-contain` and bottom fade-shadow gradient to Elements/Compounds/Custom scroll lists. Added `⤢` expand-to-full-screen button per column (mobile only, `sm:hidden`); tapping it opens a fixed full-screen sheet with Escape-to-close. Svelte 5 snippets eliminate list-item code duplication between inline card and sheet. All 1355 tests pass, ESLint clean.
  - **Log:** [log](docs/ai-logs/2026-05-16-mobile-nav-material-polish.md)
- 2026-05-16 — **entity-selection**: **Retire SelectedPill + MultiProgramPicker; add collapseToSingle** (Claude Sonnet 4.6 via Claude Code): Deleted `selected-pill.svelte` and `multi-program-picker.svelte` (and their tests). Inlined the pill markup in particle-tab, material-tab, and program-tab. Added multi-select UI to program-tab (chips + checkmarks), gated on `showAdvancedToolbar`. Added `collapseToSingle()` and `setMultiProgram()` to `EntitySelectionState`. Wired `entityState.multiSelected.program` ↔ `multiProgState` in the calculator page. Added `$effect` that calls `collapseToSingle()` on basic-mode entry so only the anchor particle/material/program survives mode switch. Added 7 unit tests for `collapseToSingle`. All 1355 tests pass, ESLint clean.
  - **Log:** [log](docs/ai-logs/2026-05-16-retire-selected-pill-multi-program-picker.md)
- 2026-05-16 — **entity-selection**: **PR #523 review follow-ups for compare-across materials/particles** (GPT-5.5 via GitHub Copilot coding agent): fixed multi-entity calculation behavior for mixed built-in/custom/external IDs with explicit per-entity errors instead of silent skips; corrected material/particle anchor disable semantics; resolved material multi-pill labels against full lists; gated multi-program WASM effect to `across === "program"`; wired per-material density conversion in multi-entity ResultTable; and added regression tests for multi-entity headers/cells/errors plus picker accessibility/filtering behavior.
  - **Log:** [log](docs/ai-logs/2026-05-16-compare-across-review-followups.md)
- 2026-05-16 — **entity-selection**: **Compare-across Materials / Particles end-to-end** (Claude Sonnet 4.6 via Claude Code): Enabled Materials and Particles in the `picker-compare-across` dropdown. Added `MultiEntityState` (`multi-entity.svelte.ts`) for lightweight per-entity result storage. Wired multi-select UI in `particle-tab.svelte` and `material-tab.svelte` (pills, ✓ indicators, anchor labeling). Added debounced WASM calculation effects in `calculator/+page.svelte` iterating over `multiSelected[dim]`. Extended `ResultTable` with grouped two-row multi-entity headers and per-entity STP/CSDA cells. All tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-16-compare-across-materials-particles.md)
- 2026-05-16 — **entity-selection**: **PR #522 review follow-up for drag-handle reorder** (GPT-5.5 via GitHub Copilot coding agent): aligned external-row drop highlight spacing with built-in rows (`pl-0`), converted drag handles from `span[role="button"]` to semantic `<button>` handles with `aria-describedby` wired to keyboard instructions, scoped `aria-live` under picker root, and adjusted keyboard up-boundary logic to derive the movable floor from actual default placement in `programDisplayOrder`; added unit assertions for handle hint wiring and live-region scoping.
  - **Log:** [log](docs/ai-logs/2026-05-16-drag-handle-reorder.md)
- 2026-05-16 — **entity-selection**: **Drag handle reorder in multi-program-picker** (Claude Sonnet 4.6 via Claude Code): Added ⋮⋮ drag handle on each selected non-default program row in `multi-program-picker.svelte`. Handle supports mouse drag-and-drop (HTML5 DnD API), touch drag (`touchstart`/`touchmove`/`touchend` + `elementFromPoint`), and `Alt+ArrowUp`/`Alt+ArrowDown` keyboard reorder when focused. Always-present `aria-live="polite"` region announces "Moved Program X to position N of M." Added 14 unit tests covering visibility, keyboard reorder (including edge-case blocking), and announcement content. All 1359 tests pass, ESLint clean.
  - **Log:** [log](docs/ai-logs/2026-05-16-drag-handle-reorder.md)
- 2026-05-16 — **entity-selection**: **PR #521 review follow-ups for load-external modal** (GPT-5.5 via GitHub Copilot coding agent): fixed stale-label handling by validating against service-cached labels and rejecting label/source mismatches in `ExternalDataService`; rebuilt calculator external context with post-update selection reconciliation on add/remove; made recents persistence best-effort; restricted URL modal validation to HTTPS `.webdedx` (with localhost HTTP exception); rendered local directory sources as non-link text in the panel; made `externalDataQuerySegments` scheme checks case-insensitive; refactored `FileSystemDirectoryHandleStore` to preserve range reads via `File.slice()` and rethrow aborts; added focused tests for modal flows, store-override loader path, directory-store caching/eviction, URL filtering, and local-source panel rendering; synced `docs/ai-logs/README.md` index entry for the original load-external log.
  - **Log:** [log](docs/ai-logs/2026-05-16-load-external-review-followups.md)
- 2026-05-16 — **entity-selection**: **Load-external modal: URL validator + drag-drop + recents** (Claude Sonnet 4.6 via Claude Code): Built `load-external-modal.svelte` with URL tab (regex validator, auto-label from URL stem, recents from `webdedx.externalRecents.v1`) and Local directory tab (File System Access API drop zone, Chrome/Edge 86+). Added `ExternalDataService.loadFromUrl()` / `loadFromDirectory()`; added `FileSystemDirectoryHandleStore` (zarrita-compatible store backed by `FileSystemDirectoryHandle`); refactored `loadStoreMetadataFromStore` / `loadStpSlice` / `loadCsdaSlice` to accept optional store override; filter non-http sources from URL segments. Wired `onLoadExternal` prop through `entity-selection.svelte` → `AdvancedToolbar` (enables the formerly-disabled button). Wired runtime source-add + context rebuild in `calculator/+page.svelte`. All 1329 tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-16-load-external-modal.md)
- 2026-05-16 — **entity-selection**: **Move external-sources-panel under entity-selection; per-source × Remove** (Claude Sonnet 4.6 via Claude Code): Moved `external-sources-panel.svelte` from `src/lib/components/` to `src/lib/components/entity-selection/`. Each source row is now individually collapsible (`<details>`). Added per-source × Remove button: calls `externalDataService.evict(label)` then the parent-supplied `onRemove` callback. Both calculator and plot pages pass a handler that filters the source from `loadedExternalSources`. Added 3 new unit tests (Remove callback, evict call, per-source collapse). All 1329 tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-16-external-sources-panel-move.md)
- 2026-05-16 — **entity-selection**: **Persistent picker-level search row + chevron + dynamic placeholder** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Lifted the three per-tab `<SearchInput>` instances out of `particle-tab.svelte`, `material-tab.svelte`, `program-tab.svelte` into a single `<EntitySelection>`-owned search row (`data-testid="picker-search-row"`). Added a chevron toggle button (`▼` / `▲`, `data-testid="picker-toggle"`) on the right edge of the row that toggles `selectionState.expanded`. Placeholder switches with `activeTarget` (`"Search particles…"` / `"…materials…"` / `"…programs…"`), and the input's `data-testid` follows `picker-{activeTab}-search` so existing unit + E2E tests keep working. Particle list keyboard nav (↑/↓/↵) is preserved by registering the tab's local handlers on parent `$bindable` props.
  - **Log:** [log](docs/ai-logs/2026-05-16-picker-search-row.md)
- 2026-05-16 — **entity-selection**: **Entity-selector chrome + state rework (option 2)** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Removed `recipe-bar.svelte`; added `activeTarget` / `expanded` / `across` / `multiSelected` to `EntitySelectionState`; new `advanced-toolbar.svelte` (Compare-across — Programs enabled, others disabled; Load-external + Explore-compat disabled placeholders; `↺` reset); new `multi-list.svelte` (SELECTED + AVAILABLE, `DEFAULT` badge, drag/keyboard reorder deferred); Program tab renders `<MultiList>` when `across === "program"` in Advanced; Material tab gains coral `+ New custom material` pill in Advanced; tabs render coral active-target underline and red-dashed/`!`-badge empty styling (new `picker-tab-{id}-empty` testid). Persistent picker-level search row, load-external modal, external-sources move, multi-list reorder, and Materials/Particles compare-across wiring are tracked as follow-up issues.
  - **Log:** [log](docs/ai-logs/2026-05-16-entity-selector-rework.md)
- 2026-05-16 — **entity-selection**: **Drop "v8" codename across the picker** (GPT-5.5 via GitHub Copilot coding agent): Renamed `src/lib/components/v8/` → `src/lib/components/entity-selection/`, `EntitySelectionV8` → `EntitySelection`, `V8Tab`/`V8_TAB_ORDER` → `PickerTab`/`PICKER_TAB_ORDER`, and every `data-testid="v8-*"` → `picker-*`. Deleted the dead `picker-flag.svelte.ts` feature switch and its unit test, since the tabbed picker is the only render path. Renamed the dedicated unit and Playwright specs and updated `docs/04-feature-specs/entity-selection.md` to describe a single current picker (dropped `?v8=1` flag, folded the v7 history into a short changelog).
  - **Log:** [log](docs/ai-logs/2026-05-16-drop-v8-naming.md)
- 2026-05-16 — **entity-selection v8**: **PR #508 plot accessibility E2E follow-up** (GPT-5.5 via GitHub Copilot coding agent): Fixed the CI-only Plot axe failure by making v8 listbox option wrappers presentational and adding keyboard focus targets to scrollable listbox containers.
  - **Log:** [log](docs/ai-logs/2026-05-16-v8-plot-accessibility-ci.md)
- 2026-05-16 — **entity-selection v8**: **PR #508 E2E selector migration follow-up** (GPT-5.5 via GitHub Copilot coding agent): Fixed failing CI E2E jobs after v8 became default by replacing remaining v7 particle/program selectors with v8 test IDs, stabilizing custom-compound modal element entry, adjusting responsive expectations to the new tab bar, and preventing collapsed tab buttons from pointing `aria-controls` at unmounted panels.
  - **Log:** [log](docs/ai-logs/2026-05-16-v8-e2e-ci-followup.md)
- 2026-05-15 — **entity-selection v8**: **v8 picker: auto-collapse, Z inline, mobile tabs, make default** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Auto-collapses Calculator tab panel when all selections complete (`collapsible` prop); moves Z= atomic number inline into particle name brackets; fixes `× clear` pill alignment (ml-auto + border badge); stacks tab values below title on mobile; promotes v8 to default (removes `?v8=1` gate); updates entity-selection.spec.ts and v8-entity-selection.spec.ts for new default; rewrites complex-interactions and custom-compounds E2E for v8 UI.
  - **Log:** [log](docs/ai-logs/2026-05-15-v8-collapse-z-mobile.md)
- 2026-05-15 — **entity-selection v8**: **Migrate custom-compounds E2E spec to v8 UI** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Rewrote `tests/e2e/custom-compounds.spec.ts` to use the new v8 tab-panel material picker (`v8-tab-material`, `v8-material-add-compound`, `v8-material-search`, `v8-material-col-custom`, `v8-material-item-*`) throughout all 16 tests; removed v7 combobox selectors, stale DOM-inspection debug code, and Escape-to-close patterns; updated material selection assertions to check `v8-tab-material` text content instead of old button labels.
  - **Log:** —
- 2026-05-15 — **entity-selection v8**: **PR #508 review follow-up — selector layout polish** (GPT-5.5 via GitHub Copilot coding agent): Updated the v8 picker to reduce vertical footprint, switched Material tab from sub-tabs to side-by-side Elements/Compounds/(Advanced)Custom columns with shared filtering, moved row density inline as `(ρ=… g/cm³)`, added a visible `+ Add compound` entry point in the Custom column, tightened tab-label alignment, and changed Program TAB badge text to DATA; updated v8 tests/spec text accordingly.
  - **Log:** [log](docs/ai-logs/2026-05-15-entity-selection-v8-review-followup.md)
- 2026-05-15 — **entity-selection v8**: **PR #508 v8 E2E failure follow-up** (GPT-5.5 via GitHub Copilot coding agent): Fixed the new `v8-entity-selection.spec.ts` reset test to mutate via the always-visible alpha particle instead of assuming Air has material id 267 in the WASM-backed CI data, preserving reset coverage while removing the brittle fixture assumption.
  - **Log:** [log](docs/ai-logs/2026-05-15-entity-selection-v8.md)
- 2026-05-15 — **entity-selection v8**: **Entity-selection v8 redesign — PR #1 (foundation + Stages 1–3)** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Landed the v8 spec draft alongside v7, added a `?v8=1` feature flag and a `pickerMode` (basic/advanced) store, and built the v8 shell behind the flag — recipe bar, tab bar with keyboard nav, and Particle / Material / Program tab content with TAB/FN/EXT tag pills. v7 stays the default render path so every existing test keeps passing; new unit and E2E coverage targets the v8 surface. Stage 4 compat overlay deferred to PR #2; Stage 5 custom-compound modal / periodic grid / inline multi-program list deferred to follow-up commits.
  - **Log:** [log](docs/ai-logs/2026-05-15-entity-selection-v8.md)
- 2026-05-15 — **maintenance**: **Vendor submodule Dependabot guard fix** (GPT-5.5 via GitHub Copilot coding agent): Allowed CI guard checks to pass for scoped vendor-only gitlink bump PRs while continuing to reject mixed first-party/vendor changes and generated artifacts; added focused guard tests.
  - **Log:** [log](docs/ai-logs/2026-05-15-vendor-submodule-guard.md)
- 2026-05-15 — **maintenance**: **Dependabot JSROOT 7.11 build fix** (GPT-5.5 via GitHub Copilot coding agent): Fixed the Vite/Rolldown browser build failure caused by JSROOT resolving the Node-only `@resvg/resvg-js` native binding by aliasing it to a local browser shim; validated lint, build, and unit tests.
  - **Log:** [log](docs/ai-logs/2026-05-15-dependabot-jsroot-resvg-build.md)
- 2026-05-15 — **maintenance**: **Dependabot tooling bump CI fixes** (GPT-5.5 via GitHub Copilot coding agent): Investigated failing CI jobs for PR #497 and fixed `pnpm install --frozen-lockfile` mismatches by adding `pnpm.overrides` to root and `prototypes/wasm-preload/app` manifests to match lockfile override blocks; updated Vitest PDF mocks for Vitest 4 constructor compatibility; validated frozen install, targeted PDF tests, full unit/integration tests, and build.
  - **Log:** [log](docs/ai-logs/2026-05-15-dependabot-tooling-bump-ci-fixes.md)
- 2026-05-15 — **maintenance**: **Workflow approval guidance for Copilot/fork PRs** (GPT-5.5 via GitHub Copilot coding agent): documented that "Approve workflows to run" is controlled by repository Actions settings (not workflow YAML), including the exact Settings → Actions → General → Fork pull request workflows path maintainers must change to remove manual approvals.
  - **Log:** [log](docs/ai-logs/2026-05-15-workflow-approvals.md)
- 2026-05-15 — **maintenance**: **Dependabot smart bump grouping** (GPT-5.5 via GitHub Copilot coding agent): Grouped npm Dependabot updates across the root app and prototype package manifests, with a focused Svelte/Vite tooling group for related bumps; updated the extdata browser prototype `manualChunks` config to the Vite 8-compatible function form; documented the prototype Vite 8 pitfall.
  - **Log:** [log](docs/ai-logs/2026-05-15-dependabot-smart-grouping.md)
- 2026-05-15 — **8 (review)**: **PR #495 review follow-ups** (GPT-5.5 via GitHub Copilot coding agent): Relaxed/clarified the MinIO CORS verification to accept echoed preview-origin or wildcard responses from a real GET request; restored the public Cyfronet SRIM links in the `/docs/user-guide` examples while keeping tests free of literal Cyfronet URLs; marked PR #495 done in the Stage 8 table.
  - **Log:** [log](docs/ai-logs/2026-05-15-pr495-review-followups.md)
- 2026-05-15 — **8 (external-data CI)**: **Manual S3 E2E suite for external data** (GPT-5.5 via GitHub Copilot coding agent): Added workflow-dispatch-only MinIO-backed S3 E2E coverage with a generated synthetic `.webdedx` store, retagged the external-data advanced Playwright suite as `@s3` with `EXTERNAL_DATA_URL`, removed Cyfronet S3 URLs from active tests and example URL assertions, and linked PR #495 in Stage 8 tracking.
  - **Log:** [log](docs/ai-logs/2026-05-15-ci-s3-data.md)
- 2026-05-15 — **8 (review)**: **PR #491 review follow-ups: external data in advanced mode** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Gated E2E suite behind `@nightly` tag and excluded from default CI runs; clamped `massASnapshot` to ≥1 so electrons (massNumber=0) don't produce `totalMev=0`; replaced `as unknown as` double-cast in `resolveExtLocalIdForLabel` with a proper union parameter type; fixed ARIA listbox semantics by wrapping the External section in `role="group" aria-label="External"`.
  - **Log:** [log](docs/ai-logs/2026-05-15-external-data-advanced-review-followups.md)
- 2026-05-15 — **8 (external-data)**: **External programs in Calculator Advanced Mode** (Claude Sonnet 4.6 via Claude Code): Widened `MultiProgramState` to `EntityId` (`number \| ExtRef`), added an "External" section to `MultiProgramPicker`, wired external program calculation (via `ExternalDataService.interpolateAt`) into the multi-program `$effect`, fixed a URL-round-trip race where the URL-update effect overwrote `window.location.search` before the `multiProgState` effect could read restored programs; added 22 unit tests and 5 E2E tests (CORS-proxy hook for S3). Branch: `feat/external-data-in-advanced-mode`.
  - **Log:** [log](docs/ai-logs/2026-05-15-external-data-advanced-mode.md)
- 2026-05-15 — **8 (beta feedback)**: **Plot energy axis units for protons/electrons** (GPT-5.5 via GitHub Copilot coding agent): Updated Plot and CSV energy-axis handling so proton-only plots use `MeV`, heavy-ion plots without electrons use `MeV/nucl`, and electron-containing plots use `MeV` with heavy-ion X values converted to total MeV; added focused tests, synchronized Plot/unit specs, and linked PR #490 in the Stage 8 redesign table.
  - **Log:** [log](docs/ai-logs/2026-05-15-plot-energy-units.md)
- 2026-05-14 — **maintenance**: **Fix AI changelog table formatting** (GPT-5.5 via GitHub Copilot coding agent): Removed two extra pipe-delimited columns from the markdown table separator row that caused the entire table to render as plain text instead of a formatted table.
  - **Log:** —
- 2026-05-14 — **8 (external-data)**: **CSDA range from STP integration for external stores** (Claude Sonnet 4.6 via Claude Code): SRIM S3 stores have no `csda_range` array. Added `computeCsdaColumn()` to `units.ts` (trapezoidal ∫1/S(E) dE, null-propagating). Modified `getCsda()` in `ExternalDataService` to fall back to STP integration when `hasCsdaRange=false`, sharing the cached energy grid. Fixed pre-existing unit bug in `calculator.svelte.ts` where external CSDA g/cm² was stored without density conversion. Branch: `feat/csda-from-stp-integration`.
  - **Log:** [log](docs/ai-logs/2026-05-14-csda-from-stp-integration.md)
- 2026-05-14 — **8 (review)**: **PR #478 review follow-ups** (GPT-5.5 via GitHub Copilot coding agent): Fixed SRIM ICRU duplicate selection so incomplete rows cannot evict complete materials, expanded SRIM inspection README drop reporting to include duplicate counts/keys, resolved external plot URL-restore async race by awaiting external series restores before URL writes, added focused external preview/restore mapping unit tests, and updated Stage 8 PR tracking with #478.
  - **Log:** [log](docs/ai-logs/2026-05-14-pr478-review-followups.md)
- 2026-05-14 — **8 (data prep + plot)**: **SRIM ICRU dedup + external plot series** (Claude Sonnet 4.6 via Claude Code): Fixed `assign_material_ids` uniqueness gate; added `resolve_icru_duplicates()` for same-density dedup and density-variant handling (Mylar/Lucite/Glycerol); 20 pytest unit tests; regenerated stores. Wired external program plot series: preview via `ExternalDataService.getStp()`, dashed lines (fLineStyle=2), URL round-trip, handleAddSeries extended. All 1259 tests pass. Branch: `feat/srim-icru-dedup`.
  - **Log:** [log](docs/ai-logs/2026-05-14-srim-icru-dedup.md)
- 2026-05-14 — **8 (review)**: **PR #476 follow-up: selector overlap and real SRIM matching** (GPT-5.5 via GitHub Copilot coding agent): Fixed mutually-overlapping Calculator combobox dropdowns by closing sibling entity comboboxes when one opens; relaxed Plot desktop grid sizing to avoid selector/sidebar overflow into the plot area; strengthened material matching for real SRIM-style `Liquid Water` / `Water, Liquid` names against built-in `Water (liquid)`; updated the shared external selector fixture to exercise that no-ICRU-id path; and changed user-guide examples to render absolute same-origin URLs using `page.url.origin` plus `$app/paths.base`.
  - **Log:** [log](docs/ai-logs/2026-05-14-pr476-review-followups.md)
- 2026-05-14 — **8 (review)**: **PR #476 review follow-ups: external data spec compliance** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Fixed root-cause bug where SRIM `Water` did not merge with built-in `Water (liquid)` (276) by plumbing `MaterialEntity.rawName` and adding a parenthetical-suffix fallback in `matchBuiltinMaterial`; added 🔗 prefix + `(ext)` suffix on external programs and 🔗 prefix on external-only particles/materials per spec §7.1; replaced inline badge with a collapsible "External Data Sources" attribution panel per spec §7.3; grouped external programs in the Plot panel and marked them unavailable since plot series are not yet supported; switched user-guide example URLs to use the SvelteKit base path so they match the deployed origin; added a Plot example URL; fixed test fixture so the optional external-only particle (now Antiproton, PDG -2212, Z=-1) does not collide with built-in electron via the (Z, A) fallback; added unit coverage for material-name matching and the new attribution panel.
  - **Log:** [log](docs/ai-logs/2026-05-14-pr476-review-followups.md)
- 2026-05-14 — **8 (beta feedback)**: **External data selector docs and menu fix** (GPT-5.5 via GitHub Copilot coding agent): Added user-guide instructions with a copy-pasteable SRIM `.webdedx` URL, fixed Calculator and Plot selectors to show compatible external programs and external-only entities, added focused regression tests, and linked PR #476 in the Stage 8 redesign table.
  - **Log:** [log](docs/ai-logs/2026-05-14-external-data-selector-docs.md)
- 2026-05-14 — **maintenance**: **pnpm check fixes** (GPT-5.5 via GitHub Copilot coding agent): Resolved all Svelte/TypeScript diagnostics from `pnpm check`, scoped `svelte-check` to first-party app sources so vendor/prototype submodules are ignored, and validated check/lint/unit/build; local impacted E2E is blocked by missing generated WASM.
  - **Log:** [log](docs/ai-logs/2026-05-14-pnpm-check-fixes.md)
- 2026-05-14 — **8 (review)**: **Alpha plot recursion review follow-ups** (GPT-5.5 via GitHub Copilot coding agent): Addressed PR #474 review comments by clamping plot-grid endpoints/intermediate values to libdedx tabulated bounds, adding custom-compound range coverage, and updating Stage 8 tracking.
  - **Log:** [log](docs/ai-logs/2026-05-14-alpha-plot-review-followups.md)
- 2026-05-14 — **maintenance**: **Alpha plot recursion fix** (GPT-5.5 via GitHub Copilot coding agent): Generated plot energy grids from libdedx per-program/per-particle tabulated ranges instead of hardcoded bounds, added unit coverage and an E2E regression for the reported alpha URL, and verified with real WASM.
  - **Log:** [log](docs/ai-logs/2026-05-14-alpha-plot-recursion.md)
- 2026-05-14 — **7.5 → 8**: **Stage 7 complete / Stage 8 start** (GPT-5.5 via GitHub Copilot coding agent): Marked Stage 7 and Stage 7.5 complete in the redesign plan, linked PR #465, updated the README and `/docs` project status to enter Stage 8 open beta/user feedback, and added a Stage 7.5 progress summary.
  - **Log:** [log](docs/ai-logs/2026-05-14-stage7-complete-stage8-start.md)
- 2026-05-14 — **7.5 (review)**: **PR #465 Code Quality follow-ups** (GPT-5.5 via GitHub Copilot coding agent): Audited all unresolved `github-code-quality` review threads and applied behavior-preserving cleanup for unused external-data constants/imports, redundant null checks in calculator/entity-selection guards, and an always-true PDG defensive check.
  - **Log:** [log](docs/ai-logs/2026-05-14-pr465-code-quality-followups.md)
- 2026-05-14 — **7.5 (review)**: **PR #465 review follow-ups — external data + E2E fixes** (GPT-5.5 via GitHub Copilot coding agent): Addressed review comments for external-data OOR rows, plot URL `extdata` symmetry, ExternalDataService cap/eviction races, compatibility assumptions, calculator ref parsing, and unit-test coverage; normalized weight-fraction custom compounds; stabilized the failing sharded E2E custom-compound tests and plot loading accessibility state.
  - **Log:** [log](docs/ai-logs/2026-05-14-pr465-review-followups.md)
- 2026-05-14 — **7.5 (Slices 3–6)**: **Stage 7.5 external data app integration — Slices 3–6** (Claude Sonnet 4.6 via Claude Code): Slice 3 — entity merging (PDG/Z,A particle + ICRU/atomicNumber/name material, 20 unit tests); Slice 4 — calculator wired to ExternalDataService (energy conversion, external program routing, error/loading UI, exactOptionalPropertyTypes fixes); Slice 5 — plot page type guards for external particles/programs (preview/addSeries blocked, URL encoding narrowed); Slice 6 — ExternalSourcesBadge attribution component + plot page external loading (mirrors calculator load pattern, error banner, extdata in URL).
  - **Log:** [log](docs/ai-logs/2026-05-14-stage7-5-external-data-app.md)
- 2026-05-13 — **7.5 (Slice 1–2)**: **Stage 7.5 external data — Slice 1 (URL/ID primitives) + Slice 2 (zarrita loader + ExternalDataService)** (Claude Sonnet 4.6 via Claude Code): Created `src/lib/external-data/` module (types, ids, url, schema, errors, validation, units, interpolation, loader, service); 79 new unit tests (58 URL + 44 loader + 35 service); updated calculator/plot URL codecs and multi-program state to use `EntityId = number \| ExtRef`; wired singleton `externalDataService`. Slices 3–7 pending.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage7-5-external-data-app.md)
- 2026-05-13 — **7.5 (handoff)**: **Stage 7.5 external-data app integration handoff** (GitHub Copilot via GitHub Copilot coding agent): Stopped before app code edits at user request and documented the exact continuation plan for Claude Code, covering URL/ID primitives, zarrita loading, entity merging, Calculator, Plot, source attribution, test gates, commit boundaries, and the no-real-URL logging rule.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage7-5-external-data-app.md)
- 2026-05-13 — **7.5 (data prep)**: **Stage 7.5 external-data SRIM prep** (GitHub Copilot via GitHub Copilot coding agent): Inspected the GUI/headless websrim Arrow reference exports; documented projectile/material coverage and raw units; added a Python `srim_reference_to_webdedx.py` inspect/convert/inspect CLI; generated ignored `.webdedx` Zarr v3 LZ4 stores under `data/` for GUI (`srim-gui-2013`) and headless (`srmodule-2012`), each `[92,381,165]`, dropping 68 incomplete materials and omitting CSDA because SRIM `range_A` is projected range, not CSDA.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage7-5-external-data-prep.md)
- 2026-05-13 — **7.4 (review)**: **PR #464 review follow-ups — multi-program WASM range errors** (GPT-5.5 via GitHub Copilot coding agent): Replaced the advanced calculator multi-program range pre-check's silent return with explicit per-program `LibdedxError(101)` results for unsafe programs while continuing to calculate safe programs; added an E2E regression for MSTAR/ASTAR partial out-of-range inputs; aligned calculator/multi-program specs, Stage 7.4 progress docs, redesign-plan PR #464 notes, and lessons learned.
  - **Log:** [log](docs/ai-logs/2026-05-13-pr464-review-followups.md)
- 2026-05-13 — **7.4**: **Stage 7.4 — WASM error handling** (Claude Sonnet 4.6 via Claude Code): Per-row retry on DEDX\*ERR\*ENERGY_OUT_OF_RANGE (101) — batch fails gracefully: valid rows show results, out-of-range rows (e.g. 100 TeV for PSTAR) marked red with message; fatal calculation errors rendered below result table with "Show details" LibdedxError code toggle; plot preview WASM errors surfaced inline instead of silent console.error.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage7-4-wasm-error-handling.md)
- 2026-05-13 — **7.3 (review)**: **PR #463 E2E shard command fix** (GPT-5.5 via GitHub Copilot coding agent): Investigated the failing sharded E2E CI jobs; confirmed `pnpm test:e2e -- --shard=N/2` expands to `playwright test -- --shard=N/2`, making Playwright treat the shard flag as a test-file pattern and report "No tests found"; fixed the workflow to call `pnpm test:e2e --shard=N/2` and validated the exact pnpm script form with `--list`.
  - **Log:** [log](docs/ai-logs/2026-05-13-pr463-e2e-shard-command-fix.md)
- 2026-05-13 — **7.3 (review)**: **PR #463 E2E sharding + Stage 7.3 redesign-plan completion** (GPT-5.5 via GitHub Copilot coding agent): Investigated the successful E2E CI run (178 passed, 8 skipped, 3.2m test phase / 4m20s job); split the Playwright job into two GitHub Actions shards while preserving coverage; marked the current redesign-plan sub-stage covered by PR #463 (7.3 mobile responsive polish) complete with the PR link.
  - **Log:** [log](docs/ai-logs/2026-05-13-pr463-e2e-sharding-docs.md)
- 2026-05-13 — **7.3**: **Stage 7.3 — Mobile responsive polish** (Claude Sonnet 4.6 via Claude Code): Added custom Tailwind v4 breakpoints (tablet 600px / desktop 900px); aligned entity-selector and plot-sidebar layout to spec breakpoints; added collapsible entity-panels on mobile behind a disclosure button with selection summary; JSROOT canvas height reactive (50vh mobile / min(60vh,600px) desktop); sticky first column in result-table; touch-target audit (radio pills, combobox triggers); Playwright Pixel 5 + iPad Air device projects and responsive smoke tests.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage7-3-mobile-polish.md)
- 2026-05-13 — **docs**: **Stage 8 "Open Beta & User Feedback" inserted into redesign plan** (Claude Sonnet 4.6 via Claude Code): Discussed where to place a user-feedback/visual-polish phase; agreed on a new top-level Stage 8 (not a 7.6 sub-stage) between Stage 7 and the CI/CD deploy stage. Renumbered old Stage 8 (CI/CD deploy) → Stage 9, old Stage 9 (Legacy Removal, already done) → Stage 10. Updated cross-references in `docs/00-redesign-plan.md`, `docs/08-deployment.md`, `docs/02-tech-stack.md`, `docs/10-terminology.md`, `docs/decisions/005-shadcn-svelte-components.md`, and `docs/progress/stage-5-entity-selection.md`. AI logs left unchanged as historical records.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage8-open-beta-plan.md)
- 2026-05-13 — **7.2 (review)**: **PR #460 review-thread docs consistency fixes** (GPT-5.5 via GitHub Copilot coding agent): Aligned Stage 7.2 documentation with the final net result (JSROOT default axis fonts, no explicit `fLabelSize`/`fTitleSize` override), simplified the `plot.md` AC requirement wording, corrected progress/log summaries, and added the PR #460 link in the Stage 7.2 redesign-plan task table.
  - **Log:** [log](docs/ai-logs/2026-05-13-pr460-review-followups.md)
- 2026-05-13 — **7.2**: **Stage 7.2 — JSROOT plot styling polish complete** (Claude Sonnet 4.6 via Claude Code): Audited task 7.2 requirements against current implementation; confirmed most styling (log-log defaults, axis labels with units, line widths, color palette, grid, disabled scroll-zoom, no canvas legend) was already done in earlier stages. Trialed `fLabelSize = fTitleSize = 0.04` NDC, then reverted after visual inspection. Final outcome is JSROOT default axis font sizing with no explicit override. Resolved redesign-plan contradiction ("legend top-right" vs spec's "no JSROOT legend"). Marked 7.2 ✅.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage7-2-jsroot-plot-styling-polish.md)
- 2026-05-13 — **7.1 (review)**: **PR #459 review follow-ups — accessibility labels, CI unit fix, and Stage 7 docs** (GPT-5.5 via GitHub Copilot coding agent): Fixed force-mounted combobox popup accessible names to avoid duplicate `getByLabelText()` matches, changed the Plot axe test readiness wait to a stable heading, updated the docs page/status plan to show Stage 7.1 complete and Stage 7 in progress, and added this session log.
  - **Log:** [log](docs/ai-logs/2026-05-13-pr459-review-followups.md)
- 2026-05-13 — **7.1**: **Stage 7.1 — Playwright E2E + WASM CI complete** (Claude Sonnet 4.6 via Claude Code): Added `@axe-core/playwright` WCAG 2.1 AA checks for all four routes (zero violations); fixed 4 accessibility violations at source (aria-labelledby on combobox listbox, tabindex on scroll container, darkened muted-foreground to ≥4.5:1 contrast, role="img" on plot preview swatch); disabled `aria-required-children` for the Bits UI combobox structural limitation (documented); fixed unconditionally-skipped calculator test (stale selectors → testid-based); added @smoke tags to calculator WASM, plot page-loads, and export CSV tests (17 total). Marked 7.1 ✅.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage7-1-playwright-e2e-wasm-ci.md)
- 2026-05-13 — **6 / 7 (docs)**: **Stage 6 completion + Stage 7 sub-stage planning** (Claude Sonnet 4.6 via Claude Code): Marked 6.13 ✅ (PR #457) and whole Stage 6 ✅ complete in the redesign plan; removed stale "remaining work" comment; updated Stage 6 status paragraph; replaced Stage 7 bullet-point list with a structured 5-row sub-stage table (7.1 Playwright E2E + CI, 7.2 JSROOT styling, 7.3 mobile responsive, 7.4 WASM error handling, 7.5 external data); updated README to reflect Stage 6 completion and Stage 7 active.
  - **Log:** [log](docs/ai-logs/2026-05-13-stage6-complete-stage7-plan.md)
- 2026-05-12 — **6.13 (review 2)**: **PR #457 second review-thread fixes — URL parser strictness** (GPT-5.5 via GitHub Copilot coding agent): Applied strict `urlv` token parsing, split Calculator URL version negotiation from URL restore gating, set banner buttons to `type="button"`, added calculation-blocked E2E coverage for the mismatch banner, and corrected the Stage 6.13 AI log API narrative.
  - **Log:** [log](docs/ai-logs/2026-05-12-stage6-13-url-parser-upgrades.md)
- 2026-05-12 — **6.13 (review)**: **PR #457 review-thread fixes — URL parser upgrades** (GPT-5.5 via GitHub Copilot coding agent): Removed unrelated formatting churn, archived the opencode transcript under `docs/ai-logs/`, separated Plot URL version negotiation from restore/write gating, fixed Calculator Advanced→Basic URL canonicalization after loading an advanced share URL, gated the custom-compound E2E on WASM availability, used the material round-trip fixture in contract tests, and added focused regression coverage.
  - **Log:** [log](docs/ai-logs/2026-05-12-stage6-13-url-parser-upgrades.md)
- 2026-05-12 — **6.12 (docs)**: **Stage 6.12 documentation finalization** (GPT-5.5 via GitHub Copilot coding agent): Marked Multi-program polish complete in the redesign plan, updated README Stage 6 wording, removed the duplicate root UX review file now indexed under `docs/ux-reviews/`, verified Stage 6.12 AI log/index coverage, and added this follow-up log.
  - **Log:** [log](docs/ai-logs/2026-05-12-pr452-stage6-12-doc-finalization.md)
- 2026-05-11 — **6.12 (review)**: **PR #452 review-thread fixes — Multi-program polish** (GPT-5.5 via GitHub Copilot coding agent): Applied ResultTable review follow-ups for zero-delta formatting, `calcState` prop wiring, safe drag/drop handlers, default-column drop blocking, spec-aligned aria-live announcements, static Tailwind cursor classes, unique delta tooltip IDs/test IDs, and corrected Stage 6.12 E2E selectors/assertions. Removed the stray root `=` file and archived `session-ses_1e76.md` under `docs/ai-logs/`.
  - **Log:** [log](docs/ai-logs/2026-05-11-pr452-review-followups.md)
- 2026-05-11 — **6.12**: **Stage 6.12 completion — Multi-program polish (delta tooltip + column reorder)** (Qwen3.5-397B-A17B-FP8 via opencode): Implemented delta/% difference tooltip on multi-program comparison cells using `computeDelta()` from new `src/lib/utils/delta.ts`; format: "Δ = ±0.840 keV/µm (±1.8% vs ICRU 90)" with U+2212 minus sign; aria-describedby for accessibility. Added drag-and-drop column reordering with keyboard Alt+Arrow fallback; default program column locked (draggable=false, aria-disabled=true); visual feedback (opacity-50 on drag, border-l-2 on drop target); aria-live announcements. Added Columns... button with checkbox dropdown for column visibility toggle. All 1058 unit tests pass, lint clean.
  - **Log:** [log](docs/ai-logs/2026-05-11-stage6-12-multi-program-polish.md)
- 2026-05-11 — **6.11 (review)**: **PR #451 review-thread fixes — Stage 6.11 export advanced** (GPT-5.5 via GitHub Copilot coding agent): Applied Copilot review comments for the Stage 6.11 export work: corrected the CSV metadata type import, made Calculator PDF metadata robust for partial interpolation settings, kept external program provenance accurate without URLs, fixed browser/OS detection ordering, deferred Calculator PDF page-footers until after advanced metadata pages are added, removed Playwright debug logging, and added focused PDF/component test coverage.
  - **Log:** [log](docs/ai-logs/2026-05-11-stage6-11-review-followups.md)
- 2026-05-11 — **6.11 (finalization)**: **Stage 6.11 finalization — Plot CSV modal wiring + lessons learned + spec tick-off** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Wired Plot CSV through the shared `CsvExportModal` in advanced mode (was direct download — violated stage spec Scenario 2); added `pendingPlotCsv` state slot and `mode`-aware dispatch in `performCsvDownload`; updated the Scenario 2 E2E to assert the modal + separator/line-endings options instead of the previous "no modal" assertion; archived the opencode session transcript (`session-ses_1e92.md`) into `docs/ai-logs/2026-05-11-stage6-11-export-advanced-qwen-session.md`; added lessons-learned Entries 25–27 (`$state(null)` cannot accept property writes — wrap in `{ value: T \| null }`; set modal `mode` before `open`; never rewrite an E2E to match an implementation that diverges from the spec); marked Stage 6.11 ✅ in `docs/00-redesign-plan.md`. Validation: `pnpm test` (1044 passed), `pnpm lint` (clean), `pnpm build` (ok).
  - **Log:** [log](docs/ai-logs/2026-05-11-stage6-11-finalization.md)
- 2026-05-11 — **6.10 (review)**: **PR #445 review-thread fixes — strict custom-compound parsing** (GPT-5.5 via GitHub Copilot coding agent): Fixed custom-compound formula rendering for fractional counts below one, made element input atomic-number resolution require full integer tokens, changed Plot custom-series cleanup to iterate a snapshot, corrected the C `lround()` comment, replaced permissive custom URL `parseFloat()` decoding with strict full-token parsing in Calculator and Plot, and added focused regression tests.
  - **Log:** [log](docs/ai-logs/2026-05-11-pr445-strict-custom-compound-parsing.md)
- 2026-05-11 — **6.10 (UX)**: **Compound editor UX fixes + water H₂O E2E tests** (Claude Sonnet 4.6 via Claude Code): Made weight fraction column editable (was read-only `<span>`); added `elementTexts[]` parallel state so element symbol input normalises on blur instead of desyncing; added `switchMode()` that converts formula↔weight fractions via `computeWeightFractions`/`computeAtomCounts`; validate fraction sum ≠100% blocks Save. Added three E2E tests (water formula mode, water weight-fraction mode, fraction-sum validation); verified alpha at 5 MeV in water gives 7.9 keV/µm (physically correct). All 16 custom-compound E2E tests and 1005 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-11-compound-editor-ux-water-tests.md)
- 2026-05-11 — **6.10 (review)**: **PR #445 review-thread fixes — custom mocks and store sorting** (GPT-5.5 via GitHub Copilot coding agent): Fixed mocked custom inverse methods to use their actual `params` argument, made custom compound `create()` and `update()` sort cloned element arrays instead of mutating caller-owned arrays, and added focused regression coverage for both issues. Validation: targeted Vitest for custom compounds + service interface contract passed; `pnpm lint` passed with existing warnings only.
  - **Log:** [log](docs/ai-logs/2026-05-11-pr445-review-thread-mock-store-fixes.md)
- 2026-05-10 — **6.10 (completion)**: **Stage 6.10 completion follow-up — Custom Compounds Task 5** (GPT-5.5 via GitHub Copilot coding agent): Implemented remaining custom-compound integration in this PR: Calculator single/multi-program dispatch to `calculateCustomCompound`, inverse custom paths, transient shared-URL compounds + Save/Dismiss banner, `mat\**`URL sync, Advanced Options gating for custom density/I-value/phase, Plot custom material group + custom preview/series calculations, custom export filename markers, deterministic custom WASM mocks, and re-enabled LiF E2E smoke coverage. Cleaned banned`waitForTimeout()`usage from the custom-compounds E2E spec. Validation:`pnpm build`, `pnpm test -- --reporter=basic`(1003 passed),`pnpm lint`(warnings only), custom-compounds E2E (13 passed), and full`pnpm test:e2e`(139 passed, 4 skipped).
  - **Log:** [log](docs/ai-logs/2026-05-10-stage6-10-completion.md)
- 2026-05-10 — **6.10 (E2E)**: **PR #445 E2E follow-up — custom compound test gating** (GPT-5.5 via GitHub Copilot coding agent): Fixed the latest failed Playwright run by scoping the calculator URL carbon-selection assertion to the Particle trigger (avoids strict-mode duplicate with the force-mounted dropdown option) and skipping the LiF custom-compound calculation smoke test until Stage 6.10 Task 5 lands in the separate PR requested by the user. Validation:`pnpm build`, `pnpm test -- --reporter=basic`(1000 passed), targeted Playwright (1 passed, 1 skipped), and full`pnpm test:e2e`(138 passed, 5 skipped).
  - **Log:** [log](docs/ai-logs/2026-05-10-pr445-e2e-followup.md)
- 2026-05-10 — **6.10 (review)**: **PR #445 review-thread fixes — Stage 6.10 Custom Compounds** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Applied 13 review-thread fixes — float32 →`HEAPF64`for`calculateCustomCompound`energies/STP buffers (matches`double*`C ABI); replaced silent`(int)`truncation of`elements*atoms`with`lround()`+ non-positive rejection in`dedx_extra.c`; skipped `add-button`entries in entity-combobox`selectedItem`and`handleValueChange`(was crashing); removed all`console.log`debug spam from entity-combobox; fixed`button.svelte`bare`onclick`attribute (also unblocks the prod build); per-item delete passes the row's compound;`parseInt`→`parseFloat`in compound editor (with`step="any"`); deleted three debug-only test specs; renamed `state`→`selectionState`in the comboboxes unit test (29 cascade-failures → 0). Audited Task 5 — Calculator + Plot integration is NOT yet implemented; tracked as follow-up. Moved opencode orchestrator transcript into`docs/ai-logs/`. **Follow-up:** fixed two pre-existing failing tests in `entity-selection-comboboxes.test.ts`—`electron (id=1001) cannot be selected`(scoped`getByText`to`[role="listbox"]`via`within(...)`+ exact`"electron"`to disambiguate from`ESTAR — electrons`) and `Escape key closes the dropdown`(Bits UI`forceMount`keeps the input mounted; assert`aria-expanded`/`data-state`instead of`.toBeVisible()`). All 997 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-10-pr445-review-thread-fixes.md)
- 2026-05-10 — **6.10 (T4)**: **Stage 6.10 Task 4 — Compound editor modal + entity selection** (Qwen3.5-397B-A17B-FP8 via opencode): Fixed dropdown listbox visibility issue (PopperLayer wrapper has 0 height by design — content div is absolutely positioned); tests updated to check `div.rounded-md.border.bg-popover`instead of`getByRole("listbox")`; added density description to custom compound items for dropdown display. AC-1 passes, AC-2 partially working (compound saves/persists correctly). Created `CompoundEditorModal.svelte` with bits-ui primitives, localStorage integration, form validation.
  - **Log:** [log](docs/ai-logs/2026-05-10-stage6-10-task4-compound-editor-modal.md)
- 2026-05-10 — **6.10 (T2)**: **Stage 6.10 Task 2 — WASM layer for custom compounds** (Qwen3.5-397B-A17B-FP8 via opencode): Implemented 4 custom compound WASM wrappers (`dedx_calculate_custom_forward_flat`, `dedx_get_inverse_stp_custom_compound_flat`, `dedx_get_inverse_csda_custom_compound_flat`, `dedx_get_bragg_peak_stp_custom_compound`) in `wasm/dedx_extra.{h,c}`; updated `wasm/build.sh`EXPORTED_FUNCTIONS; rebuilt WASM, verify.mjs passed (69/69); extended`LibdedxService`interface +`LibdedxServiceImpl`+ mocks with 4 methods; contract tests updated. 951 tests pass, build succeeds. Commit:`79c278b`. Branch: `qwen/stage6-10-custom-compounds`.
  - **Log:** [log](docs/ai-logs/2026-05-10-stage6-10-task2-wasm-layer.md)
- 2026-05-10 — **process**: **PR #445 follow-up — workflow guard initial-push range fix** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Investigated CI job `75085361570`, reproduced the false-positive vendor gitlink failures locally, and fixed `.github/workflows/ci.yml`so initial branch pushes diff from the default-branch merge-base instead of the empty tree; added a lessons-learned entry documenting why empty-tree range checks misfire on inherited submodules.
  - **Log:** [log](docs/ai-logs/2026-05-10-pr445-workflow-guard-initial-push-fix.md)
- 2026-05-08 — **spec-prep**: **Stage 6.10 opencode prompt authoring — Custom Compounds 5-task TDD prompt** (Claude Sonnet 4.6 via Claude Code): Audited existing code (confirmed`calculateCustomCompound`and all 4 WASM custom compound methods absent, no`mat**`URL params, no`dedx*extra.c`custom wrappers, no formula-parser or element-data utilities); produced`docs/ai-logs/prompts/2026-05-08-stage6-10-custom-compounds.md`— a 5-task TDD prompt for Qwen3.5-397B on branch`qwen/stage6-10-custom-compounds`. Tasks: (1) element-data table, formula-parser, weight-fraction converter, custom-compounds store (localStorage CRUD + validation + migration + `getCompatiblePrograms`); (2) WASM C wrappers (`dedx_calculate_custom_forward_flat`, three inverse/bragg-peak variants) + WASM rebuild + TS service interface (`calculateCustomCompound`, `getPlotDataCustomCompound`, `getInverseStpCustomCompound`, `getInverseCsdaCustomCompound`, `getBraggPeakStpCustomCompound`) + mock updates; (3) URL codec (`material=custom`+`mat*\_`params, round-trip guarantee,`fromUrlWarning` recovery); (4) compound editor modal (`CompoundEditorModal.svelte`) + entity selection integration (Calculator "Custom Compounds" group, Plot "Custom" sub-list, program compatibility filter); (5) Calculator + Plot page integration (dispatch, Advanced Options disabling, URL sync, from-URL banner, Basic-mode fallback, inverse lookup custom variants) + export behavior (CSV filenames, PDF composition table).
  - **Log:** [log](docs/ai-logs/2026-05-08-stage6-10-custom-compounds-prompt.md)
- 2026-05-08 — **process**: **PR #444 review follow-ups — Stage 6.10 preflight doc consistency fixes** (Claude Sonnet 4.6 via GitHub Copilot coding agent): addressed review-thread comments by clarifying delete URL cleanup (`material=custom`+`mat\__`removal), adding missing-reference banner testid in acceptance scenario, fixing normalized-name canonicalization rule consistency, making WASM capability audit conditional with explicit`N/A`for non-WASM tasks across implementer docs/prompts, and synchronizing AGENTS/opencode-setup reviewer model and step-limit references.
  - **Log:** [log](docs/ai-logs/2026-05-08-pr444-review-followups.md)
- 2026-05-08 — **process**: **Stage 6.10 preflight docs/process hardening (spec-only, no product feature code)** (Claude Sonnet 4.6 via GitHub Copilot coding agent): strengthened`custom-compounds.md`with implementation-gate preflight sections (acceptance scenarios, data model, validation matrix, WASM boundary, persistence/migration, test plan); enforced WASM capability discovery checklist in implementer-facing docs; switched opencode guidance to local commit-only by default (no push unless explicitly requested); expanded forbidden generated-artifact guard patterns; updated AI logging indexes.
  - **Log:** [log](docs/ai-logs/2026-05-08-stage6-10-custom-compounds-preflight-hardening.md)
- 2026-05-08 — **process**: **PR #443 follow-up — workflow guard shallow checkout fix** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Fixed`workflow-guards` CI failure (`fatal: Invalid revision range ...`) by setting `actions/checkout@v6`to`fetch-depth: 0`in`.github/workflows/ci.yml`, ensuring base/head SHA objects are present for range diffs in `scripts/guard-forbidden-files.cjs`.
  - **Log:** [log](docs/ai-logs/2026-05-08-pr443-workflow-guard-fetch-depth-fix.md)
- 2026-05-08 — **process**: **Stage 6.10 preflight hardening — generated-file guard + inverse ABI manifest + CI workflow gates** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Added `scripts/guard-forbidden-files.cjs`+`pnpm guard:staged`and CI preflight range guard against generated artifacts/vendor gitlinks; introduced`wasm/contract-manifest.json`consumed by`wasm/verify.mjs`; added `scripts/check-wasm-contract-docs.cjs`and CI docs-drift check; updated AGENTS/implementer/reviewer/run-feature/spec template/lessons-learned for strict task decomposition, real-WASM smoke policy, non-truncated E2E failure output, and`TASK BLOCKED`semantics on push/auth failure.
  - **Log:** [log](docs/ai-logs/2026-05-08-stage6-10-workflow-hardening.md)
- 2026-05-08 — **housekeeping**: **PR #439 follow-up — revert accidental submodule pointer updates** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Reverted committed gitlink updates for`vendor/svelte`, `vendor/shadcn-svelte`, and `vendor/zarrita`so the PR modifies only core repository files as requested.
  - **Log:** [log](docs/ai-logs/2026-05-08-submodule-pointer-revert.md)
- 2026-05-08 — **6.9**: **Stage 6.9 done — basic-mode tab fallback fix + Scenario 4 E2E test** (Claude Sonnet 4.6 via Claude Code): Found that switching from advanced mode (Range tab active) back to basic mode left Range content visible —`{#if inverseLookupState.activeTab === "csda"}`in the template was not gated on`isAdvancedMode.value`, so the Range/STP sections rendered even in basic mode. Fixed by adding `isAdvancedMode.value &&`guards to the Range and STP`{#if}`blocks, and`!isAdvancedMode.value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |                                                                                  | `to the Forward block. Also added`$effect`to reset`activeTab → "forward"` on mode switch (for URL/state consistency). Added the missing Scenario 4 E2E test ("switching to Basic while on Range tab shows Forward content"). 126/126 E2E pass. Marked 6.9 ✅ in redesign plan.
  - **Log:** [log](docs/ai-logs/2026-05-08-stage6-9-done-basic-mode-fallback.md)
- 2026-05-08 — **6.9**: **Stage 6.9 review fixes (round 3)** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Applied PR #439 review-thread follow-ups — strict full-number validation for inverse range/STP parsers (`1e`, `1..2`now rejected), strict numeric matching in`parseLengthInput`, YAML snippet nesting fix in `docs/ai-logs/prompts/2026-05-06-stage6-7-build-info-badge.md`, and regression tests for malformed numeric parser inputs.
  - **Log:** [log](docs/ai-logs/2026-05-08-stage6-9-review-fixes-r3.md)
- 2026-05-08 — **housekeeping**: **Session log archival + WASM binary cleanup** (Claude Sonnet 4.6 via Claude Code): Removed `static/wasm/libdedx.mjs`and`static/wasm/libdedx.wasm`from git — these are explicitly listed in`.gitignore`("never commit built binaries") but were accidentally committed in`72dcc50`. Archived raw Qwen session transcript (`session-ses_1fcf.md`, 5900 lines) to `docs/ai-logs/2026-05-07-stage6-9-inverse-lookups-qwen-session.md`and updated`docs/ai-logs/prompts/README.md`index with the qwen-session link.
  - **Log:** [log](docs/ai-logs/2026-05-08-housekeeping-wasm-cleanup-session-log.md)
- 2026-05-08 — **6.9**: **Stage 6.9 Inverse STP — WASM bisection bug (monotone curves + side-branch mismatch)** (Claude Sonnet 4.6 via Claude Code):`dedx_get_inverse_stp`in`dedx_tools.c`calls`find_min()`which returns -1 for monotone STP curves (no Bragg peak in range, e.g. ICRU 49). The bisection then used -1 as an energy endpoint, producing negative intermediate energies and a spurious WASM error → red "—" for all ICRU 49 inverse STP lookups. Additionally,`side∈{0,1}` both mapped to the high-energy branch in the C code (`side < 0`required for low branch), so PSTAR never returned the low-branch solution. Fixed`dedx_get_inverse_stp_flat`in`wasm/dedx_extra.c`with sample-based peak detection (40 log-spaced points) and two-branch bisection. Rebuilt WASM. Fixed E2E test regexes to match keV labels; added`parseEnergyMeV`helper; added 1 keV/µm ICRU49 @smoke test. 834 unit + 10 E2E pass.
  - **Log:** [log](docs/ai-logs/2026-05-08-inverse-stp-bisection-fix.md)
- 2026-05-08 — **6.9**: **Stage 6.9 Inverse STP — keV/µm unit conversion factor bug** (Claude Sonnet 4.6 via Claude Code):`stpToMevCm2g`had a factor-of-100 error in the`kev-um` branch (`value / (10 _ density)`instead of`(value _ 10) / density`). 0.7286 keV/µm → 0.07 MeV·cm²/g (below ICRU 49 minimum) → WASM error → red `—`. Fixed in both `inverse-lookups.svelte.ts`and the local helper in`+page.svelte`. Added 8 unit tests for `stpToMevCm2g`. 834 tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-08-stp-unit-conversion-fix.md)
- 2026-05-08 — **6.9**: **Stage 6.9 Inverse Lookups — STP no-results bug + unit-change recalculation fix** (Claude Sonnet 4.6 via Claude Code): Fixed two bugs: (1) Inverse STP showed nothing when WASM returned errors (out-of-range stopping powers) because `status`stayed`"valid"`with null energies and the template had no fallback — fixed by marking rows`"no-solution"`in the effect and adding`{:else}`fallbacks to both STP result cells; (2) changing the Range or STP unit selector didn't trigger recalculation because`setRangeMasterUnit`/`setStpMasterUnit`didn't propagate the new unit to existing rows, and the`$effect`s didn't register the master-unit state as reactive deps — fixed by propagating unit to rows in both setters and adding explicit dep reads. Added 3 E2E tests: STP E\*low/E_high for 30 keV/µm @smoke, Range unit-change recalculation @regression, STP unit-change recalculation @regression. 826 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-08-inverse-lookups-bug-fixes.md)
- 2026-05-08 — **6.9**: **Stage 6.9 CI fix — WASM verify inverse-symbol contract update** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Fixed `wasm/verify.mjs` to validate flat inverse exports (`dedx_get_inverse_stp_flat`, `dedx_get_inverse_csda_flat`) and updated service coverage mapping to current wrappers (plus `getBraggPeakStp`→`dedx_get_bragg_peak_stp`). `node wasm/verify.mjs` now passes with 69/69 checks.
  - **Log:** [log](docs/ai-logs/2026-05-08-stage6-9-wasm-verify-contract-fix.md)
- 2026-05-08 — **6.9**: **Stage 6.9 Inverse Lookups — PR #439 review fixes + E2E verification** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Fixed inverse state reactivity (`rangeMasterUnit`/`stpMasterUnit`/`error` now reactive), corrected inverse URL encoding for per-row suffix inputs (`7.718 cm`→`7.718:cm`), removed Range-tab debug runes/DOM attrs, replaced fallback debug text with em-dash, added explicit row error handling for inverse lookup exceptions, fixed `parseLengthInput` return type (`unit`/`toCm`nullable), fixed`build-info-badge`anchor markup, corrected`convertEnergy` MeV/nucl→MeV/u branch, aligned STP data-testids (`inverse-stp-unit`, `inverse-stp-result-low/high-*`), updated prompt index session links, and added lessons-learned entry for non-silent inverse error handling. Verified with `pnpm test`, `pnpm build`, and Playwright E2E (inverse suite + full suite: 121 passed, 4 skipped).
  - **Log:** [log](docs/ai-logs/2026-05-08-stage6-9-review-fixes.md)
- 2026-05-07 — **6.9**: **Stage 6.9 Inverse Lookups — remove mock escape hatches; fix ion_a and unit-conversion bugs** (Claude Sonnet 4.6 via Claude Code): E2E tests injected `\_\_MOCK_INVERSE_CSDA_RESULTS`via`page.addInitScript`, bypassing real WASM. Cleanup exposed two more bugs: (1) flat C wrappers never set `config->ion_a`from the nucleon number —`dedx_get_inverse_csda`returns`DEDX_ERR_ION_A_REQUIRED (209)`immediately; fixed by adding`dedx_internal_get_nucleon`call before`dedx_load_config`; (2) `inverse-lookups.svelte.ts`had a duplicate`performRangeCalculation`that raced with the page`$effect`, used wrong unit conversion (`cmToGcm2` ignores row unit), and set `row.status="error"` blocking the correct result — removed. E2E tests rewritten to use real WASM with physics-grounded assertions. 826 unit tests pass, 121/121 E2E pass.
  - **Log:** [log](docs/ai-logs/2026-05-07-stage6-9-wasm-inverse-fix.md)
- 2026-05-07 — **6.9**: **Stage 6.9 Inverse Lookups — fix `*err` uninitialised-memory bug** (Claude Sonnet 4.6 via Claude Code): Inverse CSDA/STP lookups still failed with "Inverse CSDA lookup failed for range=2" after WASM recompile. Root cause: `_malloc(4)` returns uninitialised memory; `dedx_get_csda` and `dedx_get_inverse_stp` both check `*err != 0` on entry and abort immediately on garbage values. Fixed by adding `*err = 0` in both flat C wrappers before delegating to the core functions, and `HEAP32[errPtr >>> 2] = 0` before each TS loop iteration. Rebuilt WASM. 826 unit tests pass, 6/6 inverse-lookup E2E tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-07-stage6-9-wasm-inverse-fix.md)
- 2026-05-07 — **6.9**: **Stage 6.9 Inverse Lookups — WASM calling-convention fix** (Claude Sonnet 4.6 via Claude Code): Added `dedx_get_inverse_csda_flat`, `dedx_get_inverse_stp_flat`, `dedx_get_bragg_peak_stp` flat C wrappers to `wasm/dedx_extra.c`; added `-I/src/libdedx/include` to emcc step and rebuilt WASM; updated `EmscriptenModule` interface + call sites in `libdedx.ts`. 826 unit tests pass, 121/121 E2E tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-07-stage6-9-wasm-inverse-fix.md)
- 2026-05-07 — **6.9**: **Stage 6.9 Inverse Lookups — full implementation (partial)** (Qwen3.5-397B-A17B-FP8 via opencode): Implemented all 5 tasks from the prompt: WASM service interface additions (`getInverseStp`, `getInverseCsda`, `getBraggPeakStp`, `getDensity`, `convertEnergy`); calculator URL codec (`imode`/`ivalues`/`iunit`); range suffix parser + energy auto-scaling; Calculator three-tab switcher + Range tab; Inverse STP tab + multi-program layout. Post-impl: fixed `activeTab` `$state`reactivity bug (was plain`let`, needed `$state` wrapper object). **Open issues**: WASM calling convention mismatch — `_dedx_get_inverse_stp/csda` take `(ws*, cfg*, ...)` not flat IDs; `_dedx_get_bragg_peak_stp` does not exist. Tests use mock results; real WASM calls need a JS wrapper using forward `calculate()` + binary search. 3/6 E2E tests also have a Playwright assertion bug (`expect.poll().toMatch()` returns `void`).
  - **Log:** [log](docs/ai-logs/2026-05-07-stage6-9-inverse-lookups-impl.md)
- 2026-05-07 — **6.9**: **Stage 6.9 Inverse Lookups — opencode prompt authoring** (Claude Sonnet 4.6 via Claude Code): Drafted `docs/ai-logs/prompts/2026-05-07-stage6-9-inverse-lookups.md` — a 5-task TDD prompt for Qwen3.5-397B on branch `qwen/stage6-9-inverse-lookups`. Tasks: (1) WASM service interface additions (getInverseStp, getInverseCsda, getBraggPeakStp, getDensity, convertEnergy) + mock updates; (2) calculator URL codec (imode/ivalues/iunit round-trip); (3) range suffix parser + energy auto-scaling utilities; (4) Calculator tab switcher + Range tab (single-program) with E2E Scenarios 1, 2, 4, 6, 7; (5) Inverse STP tab + multi-program column layout + export filenames with E2E Scenarios 3, 5, 7, 8.
  - **Log:** [log](docs/ai-logs/2026-05-07-stage6-9-inverse-lookups-prompt.md)
- 2026-05-07 — **spec-prep**: **Spec docs: PR review fixes round 2** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Six fixes — `docs/04-feature-specs/README.md` version sync for `inverse-lookups.md` (v5→v6) and `custom-compounds.md` (v2→v3); restored missing Plot export log link in `docs/00-redesign-plan.md` row 6.6; added `**Model:**` attribution line to three session logs missing it; fixed `page.blur(selector)` → `page.locator(selector).blur()` in `inverse-lookups.md` Scenario 1 E2E snippet.
  - **Log:** [log](docs/ai-logs/2026-05-07-spec-docs-review-fixes-r2.md)
- 2026-05-07 — **docs**: **Redesign plan: Stage 6 table consolidation + Stage 7 external data** (Claude Sonnet 4.6 via Claude Code): Merged "Completed sub-stages" and "Remaining sub-stages" into a single `#### Sub-stages` table with unified schema; fixed 6.7 (Build Info badge) appearing in the wrong split table; updated spec versions (6.9 → Final v6, 6.10 → Final v3); removed "Notes on deferred items" bullet section (info absorbed into table Notes column); updated recommended order (6.7 now done); updated Stage 7 heading and added External Data task (`external-data.md`, `extdata=` param, `ext-ref` grammar, Spike 4 gate) per `stage-6-13-url-parser.md` deferral notes.
  - **Log:** [log](docs/ai-logs/2026-05-07-redesign-plan-stage6-table-fix.md)
- 2026-05-07 — **spec-prep**: **Spec prep: inverse-lookups.md v6 — expanded acceptance scenarios** (Claude Sonnet 4.6 via Claude Code): Added Scenarios 5–8 to `inverse-lookups.md`: Scenario 5 (@smoke) exercises the third user story — 30 keV/µm proton/water produces distinct E low and E high, then 85 keV/µm (above Bragg peak) returns `"—"` for both branches; Scenario 6 (@regression) tests `m` suffix parsing (30 m proton/air ≈ 120–130 MeV) and `km` rejection; Scenario 7 (@regression) covers negative/zero/non-numeric input validation for both tabs plus out-of-tabulation-range detection on the Range tab; Scenario 8 (@regression) verifies column-per-program layout for both Range and Inverse STP in multi-program mode. Two new data-testids added to the Appendix: `inverse-range-row-error-{i}` and `inverse-stp-row-error-{i}`. Status bumped to Final v6.
  - **Log:** [log](docs/ai-logs/2026-05-07-inverse-lookups-scenarios-v6.md)
- 2026-05-07 — **spec-prep**: **Spec prep: custom-compounds.md v3 + spec TOCs** (Claude Sonnet 4.6 via Claude Code): `custom-compounds.md` → Final v3: added clickable TOC; explicit Basic-mode gating for Plot page (§2.4) with series-removal-on-switch rule; density ≤ 25 g/cm³ validation (§4.2) with Osmium rationale; atom count ≤ 1000 cap for formula mode (§4.4); Reactive Triggers Matrix column headers mark Plot columns as Advanced-only with explanatory note; Scenario 1 replaced with LiF-pellet smoke test (from user story); Custom Water retained as Scenario 2; Scenario 5b (density > 25 E2E) and Scenario 6 (Calculator→Plot cross-page availability + Basic-mode guard) added; `plot-compound-group` data-testid added to appendix. TOCs also added to `advanced-options.md`, `calculator.md`, and `inverse-lookups.md`.
  - **Log:** [log](docs/ai-logs/2026-05-07-custom-compounds-spec-v3.md)
- 2026-05-06 — **spec-prep**: **Spec prep: PR review fixes for Stage 6.11–6.13 companion specs** (Claude Sonnet 4.6 via GitHub Copilot coding agent): Applied five PR review fixes — grammar correction in `stage-6-13-url-parser.md`; `urlv` round-trip table disambiguation; `stage-6-12-multi-program-polish.md` Scenario 1 updated to `{programId}` data-testid convention; `stage-6-11-export-advanced.md` out-of-scope §6.3 reference corrected to §6.1; separator row added to this changelog table.
  - **Log:** [log](docs/ai-logs/2026-05-06-spec-prep-review-fixes.md)
- 2026-05-06 — **spec-prep**: **Spec prep: upgrade Stage 6.9–6.13 specs for opencode** (Claude Sonnet 4.6 via Claude Code): Amended `inverse-lookups.md` → Final v5 and `custom-compounds.md` → Final v2 (added Given/When/Then acceptance scenarios with data-testid DOM observables, reactive triggers matrix, cross-page parity checklists); created three focused companion docs: `stage-6-11-export-advanced.md` (CSV config modal, PNG export, Calculator PDF), `stage-6-12-multi-program-polish.md` (drag-and-drop column reordering + delta tooltip), `stage-6-13-url-parser.md` (urlv version negotiation, material=custom round-trip). 47 new data-testid anchors defined across all 5 specs. Updated feature-specs README with companion doc table. Branch: `docs/spec-prep-6-9-to-6-13`.
  - **Log:** [log](docs/ai-logs/2026-05-06-spec-prep-6-9-to-6-13.md)
- 2026-05-06 — **deps**: **TypeScript 6.0.3 dependabot — make PR mergeable after rebase** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Rebase regressed `tsc --noEmit` (now wired into CI). TS 6 surfaces two new strictness checks: `noUncheckedIndexedAccess` now applies to TypedArrays, and `exactOptionalPropertyTypes` rejects `{ ...maybeUndefined, x }` when the spread might erase a required key. Fixed `csda-integration.ts` / `libdedx.ts` Float32Array indexing with `!` (in-bounds loop invariants); changed `AdvancedOptions.interpolation.{scale, method}` to individually optional in `wasm/types.ts` (preserves the "defaults aren't in the URL" round-trip contract — reverted my earlier partial-default attempts in `advanced-options.svelte.ts` / `plot-url.ts`); added `'in'` narrowing on `SelectedProgram` in `entity-selection.svelte.ts` + one test; added missing `WithElementRef`/`WithoutChildren` helpers to `$lib/utils.ts`(consumed by shadcn input/skeleton/button); restructured`button.svelte`re-exports to satisfy`no-import-assign`; added `src/app.d.ts`for`$app/paths`; trimmed `ExportEntity` literals in `export.test.ts`; added `@ts-expect-error` for jsdom 29's missing types in `tests/setup.ts`; relaxed `@typescript-eslint/no-non-null-assertion` project-wide (TS 6 makes `!` the most readable way to express in-bounds-loop and TypedArray invariants); applied `]\.` → `]!.` rewrite across all test files. `tsc --noEmit` clean, 750 unit tests pass, build succeeds.
  - **Log:** [log](docs/ai-logs/2026-05-06-typescript-6-merge-readiness.md)
- 2026-05-06 — **post-mortem**: **PR #427 post-mortem: process, tooling, docs, contract tests** (GitHub Copilot coding agent): Added `.opencode/lessons-learned.md` (14 entries from PR #427); updated implementer.md + opencode.json (Step 8a/8b, maxSteps→40, output contract with What-changed+Smoke-test); updated reviewer.md + opencode.json (model→Qwen3.5-397B, 5 new diff checks, smoke E2E permission); added `docs/04-feature-specs/_template.md` with 7 mandatory sections; updated ESLint config (no-restricted-imports warn, waitForTimeout error); updated playwright.config.ts (@smoke/@regression/@nightly tags); added test:e2e:smoke/nightly scripts; tagged 2 existing tests @smoke; fixed pre-existing waitForTimeout violations in 3 E2E specs; added 24 contract tests (url-codec, page-init, service-interface); updated AGENTS.md with lessons-learned MUST READ.
  - **Log:** [log](docs/ai-logs/2026-05-06-pr427-postmortem.md)
- 2026-05-06 — **6.7**: **Stage 6.7: draft opencode prompt for Build Info Badge** (Claude Sonnet 4.6 via Claude Code): read `build-info.md` Final v1, existing footer in `+layout.svelte` (noted `base` import for sub-path fetch), and reference prompt schema; produced `docs/ai-logs/prompts/2026-05-06-stage6-7-build-info-badge.md` with 4 tasks: `scripts/deploy.js` (git → `static/deploy.json`, `stripHeadsPrefix` unit-testable); `BuildInfoBadge.svelte` component (`$effect`fetch, silent fallback); layout + CI wiring; Playwright E2E suite (6 tests,`page.route()`mock,`expect.poll`pattern, no`waitForTimeout`).
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-7-build-info-prompt.md)
- 2026-05-06 — **6.7**: **Stage 6.7: Build Info Badge — full implementation** (Qwen3.5-397B-A17B-FP8 via opencode): Implemented all 4 tasks from prompt: (1) `scripts/deploy.cjs`+ unit tests for`stripHeadsPrefix`(4 tests) — generates`static/deploy.json`with git metadata at build time; (2)`BuildInfoBadge`component — fetches`deploy.json`with`$effect`, renders commit/date/branch with GitHub link, silent 404/invalid-JSON fallback; (3) Footer integration in `+layout.svelte` + CI pre-build step in `deploy.yml`; (4) Playwright E2E suite (6 tests) — visibility, link attributes, footer location, 404/malformed-JSON absence. All 729 unit tests pass, 6 E2E tests pass, build succeeds. Added `static/deploy.json` to `.gitignore`. Commits: `6e55ade`, `62e6a26`, `dc1dce6`, `df18252`, `d7491c9`.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-7-build-info.md)
- 2026-05-06 — **6.8**: **Stage 6.8: fix plot page `?mode=advanced` URL param not activating Advanced mode** (Claude Sonnet 4.5 via GitHub Copilot coding agent): `plot/+page.svelte` was never calling `initAdvancedModeFromUrl`, so navigating to `/plot?mode=advanced` left `isAdvancedMode.value = false` and the `AdvancedOptionsPanel` was never rendered; E2E tests in `plot.spec.ts` timed out waiting for `button:has-text("Advanced Options")`. Fix: imported `initAdvancedModeFromUrl` and called it synchronously (before the async `getService().then(...)`) inside the URL init `$effect`, mirroring the calculator page pattern. 717 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-8-e2e-mode-url-fix.md)
- 2026-05-06 — **6.8**: **Stage 6.8: second PR review fixes** (Claude Sonnet 4.5 via GitHub Copilot coding agent): `plot/+page.svelte` `replaceState`wrapped in`untrack()`to prevent`effect_update_depth_exceeded`loop;`.svelte.ts`import specifier normalized to`.svelte`in`advanced-options-panel.svelte`and`calculator/+page.svelte`; E2E `waitForTimeout(500)`replaced with`waitForFunction`on URL`density=2`condition;`advanced-options-panel.test.ts`rewritten to use`advancedOptions`module singleton (component never accepted`options`prop). 717 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-8-review-fixes-2.md)
- 2026-05-06 — **6.8**: **Stage 6.8: plot density reactivity fix + CI auto-run** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Root cause:`plot/+page.svelte`preview`$effect` read `advancedOptions.value` (object ref) but not nested properties synchronously — so Svelte's fine-grained tracker did not register deps on `densityOverride` etc. Fix: added `advOptsKey` derived (same pattern as calculator) and read `_advOptsKey` + `advancedModeActive` synchronously in the preview effect. Also added `data-testid="preview-series"` + `data-density` attribute to preview list item for E2E testability. New E2E test suite (6 tests): density changes preview `data-density`, URL persistence, clear-reverts-to-built-in, MeV·cm²/g no-error, interpolation method URL update. CI: added `workflow_dispatch` trigger and `concurrency` group (`cancel-in-progress: true`) to eliminate stale-run queuing. 717 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-06-plot-density-fix.md)
- 2026-05-06 — **6.8**: **Stage 6.8: density override isolation + Basic mode revert fix + CI auto-run** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Fixed two bugs: (1) multi-program `result-table.svelte` now uses `advancedOptions.value.densityOverride` for density conversion (was always using built-in material density); (2) `calculator.svelte.ts` and `plot/+page.svelte` now guard all advanced-option reads with `isAdvancedMode.value` so Basic mode never applies density/aggregate-state overrides. Plot preview series `density` field uses the override only in Advanced mode. `calculator-state.test.ts` updated — advanced-mode tests now set `isAdvancedMode.value = true`; new test asserts override is NOT applied in Basic mode. E2E tests rewritten to check `td[data-program-id][data-testid^="range-cell-"]` (the correct multi-program cells in Advanced mode, not the absent `range-cell-0`); new E2E test: Basic-mode revert after density override. CI: added `feat/**` to push branch triggers. 717 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-8-density-override-basic-mode-fix.md)
- 2026-05-06 — **6.8**: **Stage 6.8: agent instructions, E2E timeouts, redesign plan — Stage 6.8 marked complete** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Updated `.opencode/agents/implementer.md` with mandatory rules for reactive dep registration in `$effect` (synchronous snapshot pattern), E2E test timeout guidance (`waitForSelector`/`waitForFunction`/`expect.poll`over`waitForTimeout`), reactive side-effect E2E test pattern; added playwright.config.ts global timeout (60 s), actionTimeout (10 s), navigationTimeout (30 s), expect.timeout (10 s); marked Stage 6.8 as ✅ complete in `docs/00-redesign-plan.md`; removed 6.8 from remaining sub-stages; updated recommended order.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-8-agent-instructions-and-cleanup.md)
- 2026-05-06 — **6.8**: **Stage 6.8: density override reactive trigger fix + E2E tests** (Claude Sonnet 4.5 via GitHub Copilot coding agent): Fixed root cause — calculation was never retriggered when `advancedOptions`changed.`calculator/+page.svelte`gains a`$effect` that reads `advOptsKey` and calls `calcState.triggerCalculation()` in basic mode; the multi-program effect also reads `advOptsKey` (retriggers in advanced mode) and snapshots `advancedOptions.value` before the `setTimeout` closure. Plot page preview `$effect`now snapshots`advancedOptions.value`synchronously so it re-runs when options change. Added E2E tests: density 2× → CSDA range decreases; density 2× → keV/µm increases; Advanced Options panel absent in Basic mode (both calculator and plot pages). 716 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-8-density-advanced-mode-fix.md)
- 2026-05-06 — **6.8**: **Stage 6.8: density override not applied + Advanced Options panel gating fix** (Claude Sonnet 4.5 via GitHub Copilot coding agent):`calculator.svelte.ts`imported`advancedOptions`, passes it to `service.calculate()`, and uses `densityOverride`in keV/µm and range-to-cm conversions;`getStpDisplayUnit()`now respects`aggregateState`override (spec Behavior §3); plot page`AdvancedOptionsPanel`gated with`isAdvancedMode.value &&`(AC-1); mock`calculate()`updated to accept`\_options?`; 8 new unit tests for density override and aggregate-state coupling. 716 unit tests pass.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-8-density-advanced-mode-fix.md)
- 2026-05-06 — **6.8**: **Stage 6.8 Advanced Options: apply PR review fixes** (Claude Sonnet 4.5 via GitHub Copilot coding agent): 10 targeted fixes from copilot-pull-request-reviewer: stale `const options`→`advancedOptions.value`; `cn`import`.js`extension;`getPlotData`interface+impl now accept`options?: AdvancedOptions`; `mstar_mode`decoder expanded to all valid modes (a/c/d/g/h); density/ival validation now uses`Number.isFinite`+ ival ≤ 10000 cap; calculator page`AdvancedOptionsPanel` props fixed (`materialBuiltInDensity`, aggregate state, selectedProgram name vs id); localStorage persistence added to calculator; plot page `loadAdvancedOptionsFromStorage`no longer gated on`wasmReady`; `getMaterials`uses`resolvedProgramId`not`selectedProgram.id`(-1 Auto-select bug fixed) with cancel guard; removed invalid`options`prop from both panel call sites;`pnpm format`on csda test. 709 unit tests pass, build clean.
  - **Log:** [log](docs/ai-logs/2026-05-06-stage6-8-review-fixes.md)
- 2026-05-05 — **6.8**: **Fix Select components — bits-ui`onValueChange`never fires in Svelte 5** (Qwen3.5-397B-A17B-FP8 via opencode): bits-ui Select's`boxWith()`binding doesn't trigger`onValueChange`or update`bind:value`in Svelte 5 despite UI updating. Root cause: bits-ui uses`boxWith(() => value!, (v) => { value = v; onValueChange(v); })`where setter is never called. Solution: replaced bits-ui Select with native`<select>`wrapped in`NativeSelect`component. Files: created`src/lib/components/ui/native-select/native-select.svelte`(47 lines,`onchange`handler,`options`prop),`index.ts`; removed `src/lib/components/ui/select/select.svelte`; updated `advanced-options-panel.svelte`(3 Select → NativeSelect); fixed`advanced-options.spec.ts` E2E test (`page.selectOption()`instead of clicking`[role="option"]`). All 96 E2E tests pass. Scale select updates URL with `interp_scale=lin-lin`, method select updates with `interp_method=spline`. Both Calculator and Plot pages verified. Lint: fixed unused imports/warnings in `advanced-options-panel.svelte`.
  - **Log:** [log](docs/ai-logs/2026-05-05-stage6-8-select-fix.md)
- 2026-05-25 — **6.8**: **Fix failing E2E tests** (Antigravity via opengravity): Fixed E2E timeouts and focus issues in custom compounds, multi-entity tab navigation, and advanced modes. Fixed idempotency issue in setAcross. All 291 tests passing.
  - **Log:** [log](docs/ai-logs/2026-05-25-happy-brown-IPv9Y.md)
- 2026-05-25 — **6.8**: **Fix failing unit tests** (Antigravity via opengravity): Fixed assertion in entity-selection-tabbed.test.ts related to non-idempotent setAcross behavior. All tests passing.
  - **Log:** [log](docs/ai-logs/2026-05-25-happy-brown-IPv9Y-2.md)
